---
name: battle-logic
description: Use when working on turn flow, attacks/damage calculation, trainer cards (道具牌/支援者牌/競技場牌), status effects (中毒/燒傷/麻痺/睡眠/結凍/混亂), KO/double-KO resolution, buffs (atkBonus/atkMult/shield/reflect/typeOverride), or ability trigger hooks. Triggers on requests like "新增卡片", "修改攻擊力計算", "狀態異常有問題", "換場機制", "反彈鏡", "同時陣亡判定", "特性觸發".
---

# Battle Logic (對戰相關)

## Core rule: fix single-player AND PvP together

Every battle mechanic exists **twice**, independently implemented: `pokemon_battle.html`/`public/single.html` (client-only, CPU opponent) and `server.js` (server-authoritative) + `public/pvp.html` (thin render client). They are NOT sharing code. A fix in one almost never automatically applies to the other — treat every battle-logic change as two separate patches that happen to be conceptually identical. This has been the single most common source of "works in single-player but not PvP" bugs across every session so far.

After editing `pokemon_battle.html`, sync: `cp pokemon_battle.html public/single.html && diff` to confirm.

## Turn flow (single-player)

```
playerAttack(i) / playerSwitch(i) / playerStandby()
  └─ doAttack(player→cpu) → cpuAttack()
       └─ cpuUseTrainers()  [CPU plays trainer cards from hand first]
          └─ doAttack(cpu→player)
               └─ handleKO(side, cb) if HP ≤ 0
                    └─ showResult() if that side's whole team is wiped, else cb()
```
`G.busy=true` disables player input during animations/CPU turn; resets via the various `endTurn`-style callbacks.

Switching (`playerSwitch` → confirm modal → `executeSwitch`) **ends the turn** by default (×0.8 damage guard applied to the switched-in mon, `G.playerSwitchGuard`) — except when `G.freeSwitch` is set (granted by the 撤退背心/retreat-vest item card), in which case it skips the confirm modal and does NOT end the turn. `G.freeSwitch` was a previously-dead unused field from an older design (换人命令 card) — repurposed rather than adding a new one; if you see other suspiciously-unused `G.*` fields, check git history before assuming they're safe to reuse.

## Turn flow (PvP) — `server.js`

WS message types: `attack`, `switch`, `standby`, `ko_switch`, `use_trainer`, `discard`, `discard_trade`. Handler is one big `if (type === '...')` chain inside `handleMessage(ws, msg)`. `G.turn` (`'p1'|'p2'`) is the authoritative whose-turn-is-it flag; `G.pendingKOSwitch` (role or null) gates forced replacement after a KO — while set, most other message types are rejected.

`G.pendingKOSwitchQueue` (array) — built for 搏命/sacrifice, reused for the double-KO fix — lets **both** sides need a forced replacement in sequence: `pendingKOSwitch` is set to the first side, and once their `ko_switch` resolves, the `ko_switch` handler shifts the next role off the queue automatically. Whether `G.turn` changes during this depends on *why* both died (see Double-KO below) — don't assume it's always the same.

## Trainer cards

`TRAINERS` array (`{id, name, cat:'item'|'supporter'|'stadium', desc}`) + `applyTrainer(card, side/role, ...)` switch statement handle the simple, single-effect cards (heal, buffs, status infliction, orbs, stadiums, energy). Items: unlimited per turn. Supporters: **1/turn, no per-stage/per-game cap** (the old "2/stage(single) or 2/game(PvP)" limit was removed 2026-07-02 at the user's request — only the `SuppUsed`-per-turn flag remains; `SuppStageUsed` still increments but nothing reads it as a gate anymore), **only appear in the opening 3-card hand** (never drawn later — post-opening draws are items-only).

**傷藥 (potion) is now 3 item cards, not 4** (`potion-s`/小傷藥 removed entirely 2026-07-18, alongside a matching global rebalance — see below): `potion-m`/`potion-l`/`potion-xl` healing 40/60/80 respectively, one shared `case 'potion-m': case 'potion-l': case 'potion-xl':` block in `applyTrainer` that looks the heal amount up from a small `{...}[card.id]` map rather than near-duplicate cases. If you add a new tier, extend that map, don't add a new switch case. Single-player's CPU heal-selection AI (`useCard('nurse') || useCard(...)` inside `cpuUseTrainers()`) tries all remaining tiers best-to-worst (`potion-xl` → `potion-l` → `potion-m`) after `nurse` — update that chain too if tiers change, it's a separate list from the switch case. 復活藥/revive also dropped from 80→40 HP in the same pass (both its `desc` string and the actual `deck[di].cur = 40` in `applyTrainer`'s `case 'revive':`).

**2026-07-18 global rebalance**: user asked for the damage bump to be offset by a healing nerf — every move's `dmg` field (both engines, all 152 Pokémon, 513 damage-bearing move slots — support moves have no `dmg` field and were untouched) got **+30** via a one-shot global regex (`dmg:(\d+)` → `dmg:` + n+30, safe because that exact literal pattern only ever appears in move objects, verified by grepping for `dmg:` outside `attacks:[` context before running it — the runtime damage-calc code always uses `let/const dmg =`, never the `dmg:` object-literal shorthand). Every **flat-number heal card** got **-40** (potion-m/l/xl, revive — see above); 小傷藥/potion-s was removed outright rather than reduced, per explicit instruction. **Percentage-based heals were deliberately left alone** (poison-heal 1/8 max HP, water-absorb 1/4, tidal-heal 30%, `selfHeal` moves, roost 50%, hot-springs' flat 15/turn, curse-wave's 20 cap) — a flat -40 doesn't apply cleanly to a percentage, and the smaller flat amounts (15/20) would go negative; only card-based, single-use, large-flat-number heals were in scope. If asked to rebalance heals again, this scoping precedent (potion-family + revive only, not passive/percentage effects) is what was used — confirm with the user if a future request seems ambiguous about which heal effects it should touch.

**Mega 稜鏡塔 (`stadium-mega-prism`)** is a normal stadium card (same generic activation case as every other stadium — `G.activeStadium = card`) whose actual per-turn effect lives outside `applyTrainer`, in the same turn-start hooks used for Mega-energy accumulation — see battle-logic skill's Mega Evolution section above.

Two cards are too complex for the generic `applyTrainer` switch and are special-cased directly in `useTrainer()`/PvP's `use_trainer` handler instead:
- **搏命 (sacrifice)**: both current actives die simultaneously. Single-player: `resolveMutualKO()`. PvP: inline in the `use_trainer` handler, sets up `pendingKOSwitch`+`pendingKOSwitchQueue`. **Both engines end the card player's turn once both sides replace** (fixed 2026-07-04 — originally neither did, letting the card's user act again immediately after both replacements were picked, reported by the user as "太強了"; single-player's final callback now calls `setTimeout(cpuAttack, 700)` instead of just clearing `G.busy`, and PvP now sets `G.turn = op` in the sacrifice block, mirroring the already-correct double-KO-from-attack case a few lines below it in `server.js`). If you touch this again, treat "does the turn actually end" as the thing to verify first — it's easy to wire up the replacement-picking UI correctly while silently forgetting the handoff.
- **瘋狂博士 (mad-scientist)**: 2-step selection UI (pick own alive mon → pick opponent's fainted mon) → `transformPokemon()` mutates the chosen mon's identity **in place** (same object reference — this has UI implications, see ui-rendering skill's sprite section). PvP validates both target indices server-side before applying (never trusts client-computed results); client sends `{type:'use_trainer', handIdx, targetOwnIdx, targetEnemyIdx}`. **HP handling changed 2026-07-15**: originally carried over the transforming Pokémon's *current HP percentage* onto the new max HP (`mine.cur = round(target.hp * (mine.cur/mine.hp))`) — user feedback was that this made the card feel bad exactly when you'd want it most (using it while nearly dead just produced a nearly-dead copy of the new Pokémon, solving nothing). Changed to always fully heal on transform (`mine.cur = target.hp`), matching Mega Evolution's existing "HP全滿" precedent in this same game — status was already cleared on transform before this change, so transform now matches Mega's "full heal + status clear" behavior exactly. The "can only target the opponent's *fainted* Pokémon" restriction was deliberately left alone (considered intentional strategic depth — save the card for after someone's died — not a flaw) even though it makes the card unusable before any KO has happened; only the HP-percentage part was judged to be the actual problem.

CPU AI (`cpuUseTrainers()`, single-player only — PvP has no AI) deliberately does **not** know how to play 搏命 or 瘋狂博士 (too complex to integrate safely into the synchronous turn flow) — they'll sit unused in CPU's hand if drawn. This is intentional, not a gap to fill without being asked.

**Two follow-up bugs found right after the 2026-07-06 card pass shipped, both from the same category of mistake — a card/mechanic added to the data array but not wired into every place that needs to know about it:**
1. **New stadium cards need adding to the *generic activation* switch case too, not just the `TRAINERS` data array.** `applyTrainer`'s stadium-activation case is a hand-enumerated list of ids (`case 'stadium-training': case 'stadium-spring': ... case 'stadium-mega-prism': { G.activeStadium = card; ... }`) — adding a new `cat:'stadium'` entry to `TRAINERS` does *not* automatically make it fall into this case; forgetting to list the new id here means the card gets consumed from hand with zero effect (`G.activeStadium` never gets set), reported by the user as "陷阱卡發動了沒效果". Both new trap stadiums (`stadium-spikes`, `stadium-toxic-field`) had this exact gap in both engines.
2. **交換器 (switcher) forces an opponent switch but never called `triggerOnEnter`/`triggerOnEnterSrv`** — a pre-existing gap that predates trap stadiums (on-enter abilities like intimidate/trace were *also* silently skipped for forced switches, just less noticeable before there was a stadium mechanic that visibly depends on it) but only surfaced once the trap stadiums made the omission observable, reported as "劇毒領域好像對新上的寶可夢沒起到作用". Fixed by adding `triggerOnEnter(opDeck[newIdx], opSide)` / `triggerOnEnterSrv(opDeck[newIdx], opRole, G, log)` right after the forced switch resolves — a forced switch is exactly as much a "field entry" as a voluntary one, it just wasn't routed through the same function. **When adding a new switch-adjacent mechanic, grep for every `G.playerIdx =`/`G.cpuIdx =`/`G[role+'Idx'] =` assignment site and check each one calls `triggerOnEnter`, not just the "obvious" ones (normal switch, KO-replacement)** — this is now the 2nd time a switch path silently skipped it.

**屬性轉換 (type-orb) is deliberately listed 6× in `TRAINERS`, not once** — consolidating 13 single-type orb cards into this one card (2026-07-06 pass) meant its raw per-slot draw probability dropped from "13 cards out of the pool" to "1 card out of the pool" — roughly a 10x reduction in how often the player would see *any* type-changing tool, even though each individual draw is now strictly more useful (no longer wasted on a mismatched type). Reported by the user as "一直抽不到屬性轉換" almost immediately after the consolidation shipped. Fixed by duplicating the plain object literal 6 times in the `TRAINERS` array. This is a general trap worth remembering: **consolidating N interchangeable single-purpose cards into 1 flexible card changes its draw frequency by roughly 1/N unless you explicitly compensate for it** — the fix for "make each draw better" and the fix for "keep the same draw frequency" are two different changes, and only doing the first one is a regression on its own.

**Weighted draw system (`weight` field, added 2026-07-20)** — the second real need for weighting arrived once the card pool grew to 68 type-restricted item cards across 18 types: the user wanted type-matching item cards to draw more often than generic item cards (屬性變換/傷藥/能量補丁 etc.), and plain duplication at that scale would have bloated `TRAINERS` by hundreds of entries. Added a proper `weight` field instead: every card object may carry an optional `weight` (default `1` if absent), and every consumer of `getDrawPool()`'s result that used to do `pool[Math.floor(Math.random()*pool.length)]` now calls a shared `weightedPick(pool)` helper (`pokemon_battle.html`/`server.js` each have their own copy, right after `getDrawPool()`) that sums `c.weight ?? 1` across the pool and picks proportionally — same pattern as the fishing feature's `rollFish()`. All 68 type-restricted **item** cards (`cat:'item'` with a `type` field) got `weight:10`; generic items and all stadium cards (even the few that carry a `type` field, e.g. `stadium-evil-forest`) were deliberately left at the implicit default of `1` — the user's comparison examples were all item cards, so the weighting was scoped to item-vs-item only. **`weight:10`, not a smaller number, was necessary because of the denominator**: a single-type Pokémon's draw pool has ~30 generic item entries at weight 1 (total weight 30) against only 4-5 type-matching item cards per type — at `weight:3` (the first value tried), even 5 cards × 3 = 15 was still far below 30, so type cards were actually *less* likely than generic ones in aggregate (verified empirically via a 20,000-sample `javascript_tool` distribution test — this is exactly the kind of assumption that needs the numeric check, not just "the individual card feels rarer so weighting must be working"). `weight:10` clears this comfortably (4×10=40 > 30) across every type, verified the same way. **When weighting a whole category of cards against a fixed pool of unweighted ones, calculate the required weight from the actual pool composition (count × weight ≥ opposing total) — don't guess a small multiplier and assume it's enough.** `type-orb`'s existing ×6 duplication was left untouched (different problem — compensating for lost absolute draw count after a 13→1 consolidation, not making a category "feel more common" relative to another category) and stacks fine alongside the new `weight` mechanism.

### Card system strategic-depth pass (2026-07-06)

User feedback: type-orb cards (originally 13 separate single-type `orb-*` items) were "在棄牌" (getting discarded unused) rather than played, because (a) the specific orb drawn rarely matched the current matchup's actual weakness, and (b) switching your attack's type away from your species' real type lost STAB, so the net gain after countering a resistance was often marginal — switching Pokémon was usually more decisive. Fixed by consolidating into **one flexible `type-orb` card** ("屬性轉換") that lets the player choose the type at play-time via a picker modal, **and** by making the overridden type automatically count as the attacker's own type for STAB purposes — `isOwnType = aBuff.typeOverride ? true : (...)` in `doAttack`, both engines. No downside anymore, only upside; CPU AI picks whichever type is super-effective (`bestMult > 1`) against the current opponent, or skips the card if nothing qualifies (kept deliberately simple, matching the project's established "CPU AI stays simple" precedent).

**Type-orb needs a type-selection UI** (`#type-orb-modal`/`#type-orb-grid`, one button per `Object.keys(TYPE_ZH)` — 18 types as of 2026-07-06, not the 16 the top-of-file project overview still documents from an earlier point) — intercepted in `useTrainer()`/PvP's `useTrainer()` the same way 瘋狂博士 is (don't consume the hand card until a type is actually picked). PvP sends `{type:'use_trainer', handIdx, chosenType}`; the server validates `chosenType` against `Object.keys(EFF)` **before** splicing the card out of hand (same "validate before consuming" ordering already used for 瘋狂博士's target validation — get this backwards and a rejected pick still eats the card).

**6 new item cards**, all reuse the existing `applyTrainer` switch (no new special-cased flows needed):
- **手牌破壞 (hand-wreck)**: opponent discards one random hand card; no-ops with a log message if their hand is empty (doesn't consume nothing — the card itself is still spent).
- **能量剝奪 (energy-drain)**: opponent loses 6 energy, floored at 0.
- **一擲千金 (gamble)**: 30% chance next attack ×4 (`Math.random() < 0.3`), 70% chance self-damage of 40% max HP, floored at 1 HP (matches `energize`'s existing self-damage-floor convention — cards never outright KO their own user).
- **背水一戰 (desperate-boost)**: `atkBonus += Math.round(50 * (1 - cur/hp))` — linear scaling, +0 at full HP up to +50 near death, unlike the flat-threshold `stadium-reversal` (+30 flat below 50% HP) it sits alongside.
- **連擊 (double-strike)**: `atkMult = max(atkMult, 1.4)` **plus** a `buff.doubleStrike` flag that makes the attack's own `atk.status` chance get rolled **twice** (independent rolls) instead of once — this is the one new mechanic that isn't just a number tweak. Implemented by capturing the flag as a local (`wasDoubleStrike` client-side; server-side the buff reset happens *after* the status roll in that file's code order, so `aBuff.doubleStrike` can just be read directly at roll time) and calling the status-roll logic a second time when set. Verified via 200-trial Monte Carlo against the extracted server code: a 50%-chance status rolled twice landed at a 0.75 proc rate, matching the `1 - 0.5²` independent-rolls prediction exactly — the client uses the exact same math (wraps the single-player's shared `tryInflictStatus()` call in an `if (wasDoubleStrike)` second call; server.js has no equivalent shared function, so the status-roll block there was pulled into a local `rollStatus()` closure specifically so it could be invoked twice).
- All 6 are `cat:'item'` (repeatable, drawn every turn) rather than `cat:'supporter'` — deliberate: unlike 搏命/瘋狂博士/全力出擊 (which are one-shot, hand-only tools), these are meant to be routinely-available tactical options, consistent with how `energize`/`reflect`/`switcher` (comparable power/complexity) are already items not supporters.

**2 new "trap" stadium cards** — this game's first entry-hazard-style mechanic (real Pokémon's Spikes/Toxic Spikes, reinterpreted for a persistent stadium instead of a per-side placed hazard): **尖峰陷阱 (`stadium-spikes`)** deals 15% max HP (floored at 1, never KOs) to any Pokémon that enters the field; **劇毒領域 (`stadium-toxic-field`)** poisons any Pokémon that enters the field (skipped if it already has a status). Both apply **symmetrically to both sides** (same "global rule, not tied to whoever played the card" convention as every other stadium in this game) and are gated behind a **new `isFieldEntry` parameter on `triggerOnEnter`/`triggerOnEnterSrv`** (defaults to `true`): every genuine bench→field entry point (initial battle start, normal/free switch, KO-replacement on both sides) already called this function to fire on-enter abilities like intimidate/trace, so the trap logic (`triggerTrapStadium`/`triggerTrapStadiumSrv`) piggybacks on the exact same call sites for free — **except** 瘋狂博士's in-place transform and Mega Evolution, which mutate the *same* battle-slot Pokémon without it ever leaving the field, and now explicitly pass `isFieldEntry=false` at their `triggerOnEnter(...)` call sites so a trap stadium doesn't punish playing those cards. If you add a genuinely new "enters the field" call site in the future, it defaults to triggering traps automatically; if you add a new *transform-in-place* mechanic, remember to pass `false` explicitly or it'll incorrectly eat trap damage/status on activation.

**Second follow-up batch, same day**: user reported the fix above was found to have 2 more gaps (see the "attacker dies without landing a hit" and "缺少一份既有清單的其中一處" lessons below — same class of mistake, new instances), then asked for a further round of expansion:
- **羅馬鬥技場 (`stadium-colosseum`)**: fighting-type moves deal ×1.5 (a new `colosseumMult` factored into the final `dmg`/`damage` formula in `doAttack`, both engines, alongside `megaBoostMult`), **and** fighting-type attacks are no longer fully immune against ghost-type (`EFF.fighting.ghost` is normally `0` — `effActive`/`srvEffActive` now override that specific pairing back to `1`/neutral when this stadium is active, added right after the existing 邪惡森林/`stadium-evil-forest` override in the same function, following its exact pattern).
- **掠奪 (plunder)**: steals one random card from the opponent's hand **into your own** — different from 手牌破壞/hand-wreck, which just discards the opponent's card into nothing. Since this *adds* to the plunderer's hand, it can push them over the 5-card cap; single-player calls the existing `showDiscardModal(() => {})` if `G.playerHand.length > 5` after stealing (no-op callback since nothing needs to happen once they've discarded); PvP sets the existing `G[role+'NeedsDiscard']` flag (same field `drawForRole` already sets after a normal draw) rather than needing a new mechanism.
- **通訊封印 (comm-seal)**: opponent can't play `cat:'supporter'` cards during their *immediately next* turn. Implemented as a "promote-then-consume" pair of flags rather than trying to align with a turn-end hook: playing the card sets `G[opSide+'SupporterLocked'] = true` (persists indefinitely until consumed); the **start of that side's own next turn** (single-player: top of `drawAfterTurn`; PvP: top of `drawForRole`, which every genuine turn-start path already funnels through) promotes it to a separate `...LockedThisTurn` flag and immediately clears the original — so the lock is "live" for exactly the turn it was promoted into, and automatically off for every turn after. `useTrainer()`/PvP's `use_trainer` handler check the `...ThisTurn` flag (not the raw one) when gating supporter-card play; single-player's CPU equivalent (`cpuUseTrainers()`) reads-and-clears the raw flag directly at its own top, since CPU's trainer-card usage is one batch call per turn rather than per-click like the player's, so it doesn't need the two-flag promotion dance — read the flag once, decide, done. If you add a third "opponent skipped one of my turns" effect, decide up front whether it's more like a *batch-per-turn* consumer (CPU-AI style: read-and-clear once) or a *scattered-across-the-turn* consumer (player-UI style: needs the promote-at-turn-start pattern) — using the wrong one either double-clears too early or never clears at all.

**Two more instances of the "added a new entry to one enumerated list but not its sibling list" bug class** found while implementing the batch above (see pokemon-data skill's moveset-diversification note for the general lesson): the type-picker modal's CSS needed *both* a `display:none` base rule *and* a `.open { display:flex }` rule added to their respective shared selector lists — single-player's copy only got the first one added when `#type-orb-modal` was introduced, so the modal would open (`classList.add('open')`) but stay visually `display:none` forever; reported as "屬性轉換沒辦法點". Same root cause as the stadium-activation `switch` case gap documented in the "Card system strategic-depth pass" section above (adding a new `cat:'stadium'` entry to `TRAINERS` without also adding its id to the enumerated `case 'stadium-x': case 'stadium-y': ...` list in `applyTrainer`) — **whenever a new card/element needs to slot into an existing multi-selector CSS rule or an existing `case A: case B: case C:` list, grep every existing member of that list, not just the one that looks most relevant, and add the new one to *all* the sibling locations in the same edit.**

**Third stadium batch, same day — 4 more cards, no user-reported bugs this round** (the accumulated "grep every sibling list" discipline from the two prior batches paid off — each new stadium id was verified present in all 4 relevant spots — `TRAINERS` entry, the `case 'stadium-x': ...` activation list, `effActive`/`srvEffActive`, and the `doAttack` multiplier chain — in both files before testing, catching zero omissions this time instead of finding one after the fact):
- **魔幻空間 (`stadium-mystic-space`)**: psychic-type defenders take ×0.9 damage (new `mysticSpaceMult`, defender-type-based like `thickFatMult`/`solidRockMult`, not attacker/move-type-based) **and** lose their weaknesses — `effActive`/`srvEffActive` override any `m > 1` back to `1` when the defender is psychic, following the exact `stadium-evil-forest` pattern (grass-type immune to disadvantages) rather than the colosseum pattern (which only fixes one specific immunity pairing).
- **熔岩火山 (`stadium-lava`)**: fire-type moves ×1.1, water-type moves ×0.5 — both are **move-type-based** (`atkType`), not the attacking Pokémon's own species type, matching how `stadium-colosseum`'s fighting boost was implemented (established precedent, kept consistent rather than switching interpretations card-to-card even though the user's Chinese phrasing for different cards inconsistently says "X系寶可夢" vs "X屬性的技能" — read literally those would imply different bases, but this game already established the "move-type" reading for this class of effect and switching per-card would create inconsistent, surprising interactions).
- **海洋世界 (`stadium-ocean`)**: water-type moves cost half energy — **new stadium-based cost reduction**, no prior precedent existed (only `pressure`'s cost *increase* did). Added to `effectiveCost`/`effectiveCostSrv`, which single-player already threaded through every affordability check and deduction site, so no other call sites needed touching — but **PvP's `effectiveCostSrv` didn't take `G` as a parameter at all before this** (it only needed `opponentPoke` for the `pressure` check), so this required adding a third parameter and updating its one call site in the `attack` handler; if you extend a helper's signature like this, grep all call sites — there was only one here, but a helper with several callers would silently break at every uncalled-through site otherwise. Electric-type moves also ×1.5 damage (`oceanMult`, same move-type-based pattern as lava's fire boost).
- **莊嚴神社 (`stadium-shrine`)**: normal-type moves are always treated as maximally super-effective (`m = 2`), an unconditional override rather than a modifier — added as the last check in `effActive`/`srvEffActive` (after the colosseum/mystic-space adjustments) so it wins regardless of what any other stadium or the base type chart would have said, including forcing damage through normal-type's real immunity against ghost (`EFF.normal.ghost === 0` normally).

Verified via the same two-track approach used throughout this session: single-player live browser attacks with hand-picked matchups (verified exact expected damage against the full multiplier chain including STAB and any relevant defender ability like thick-fat, not just the new stadium term in isolation — e.g. 魔幻空間 test used 妙蛙花→胡地 for the weakness-removal check and confirmed `48×0.9=43` exactly; 熔岩火山 test incidentally exposed that 卡比獸/Snorlax has `thick-fat`, which the expected-value math had to account for, `35×1.5×0.6×1.1=34`); PvP verified by extracting `srvEffActive`/`effectiveCostSrv`/`doAttack` into a standalone Node module and asserting exact damage/cost outputs for all 4 new stadiums, matching the single-player browser results number-for-number.

**Bug found and fixed 2026-07-13**: `public/pvp.html` has its *own* client-side `effectiveCost(atk, opponentPoke)` (used purely for the attack-button display number and the local afford-check that grays out a button) — separate from `effectiveCostSrv` in `server.js`, which is what actually validates/charges the energy. When 海洋世界 was added to `effectiveCost`/`effectiveCostSrv` (the single-player function and the server function), **`pvp.html`'s client-display copy was missed** — it only ever had the `pressure` +2 term, never the ocean ×0.5 term. Net effect: during PvP with Ocean World active, the button showed/enforced the *full* (un-halved) cost while the server silently charged the *correct* (halved) cost — a display/reality mismatch, not a scoring bug (the user's phrasing was "should be the *required* energy that's halved, not just the energy actually spent" — i.e. the discount was real but invisible/not reflected in what the UI told the player they needed). **Lesson: `effectiveCost`/`effectiveCostSrv` is not a two-copy pair, it's a three-copy pair** (single-player, server, PvP-client-display) — this skill's own energy-system section above already listed "pvp.html's renderActions()/playerAttack() client-side mirrors" as call sites needing the helper, but didn't flag that the *helper itself* is duplicated a third time and must be kept in sync manually (no shared module to import from, this is a single-file-per-engine codebase). When adding a future stadium/ability that changes move cost, grep for `function effectiveCost` (should get exactly 3 hits: `pokemon_battle.html`/`public/single.html` share one via the sync check, `server.js`, and `public/pvp.html`) and update all three, not just the two you'd naturally think of (single-player + server).

## Status effects

Shape: `{type, turnsLeft}`. `turnsLeft`: sleep 2-3, confusion 2-4, freeze 2 (all random/fixed); poison/burn/paralysis use `999` (i.e. "persists until cured", not turn-limited — paralysis has no countdown at all, just a flat 50%-skip roll every attack attempt regardless of `turnsLeft`).

**Two-phase model (reworked 2026-07-03)** — status resolution is split into "does it block acting" (only relevant when *attempting to attack*) and "does it tick/deal damage at turn end" (relevant on every turn regardless of what the Pokémon did):

- **Attack-gating** (`handleAttackerStatus()` single-player / `handleStatus()` PvP, called only from `playerAttack`/`cpuAttack`/the `attack` WS handler): sleep/freeze/paralysis may block the attempt (`onSkip`); confusion may force a self-hit instead of the intended move. Poison/burn **never** resolve here anymore — they always let the attempt through untouched.
- **End-of-turn damage** (`applyEndOfTurnStatus()` single-player / `applyEndOfTurnStatusSrv()` PvP): applies poison/burn damage (or poison-heal ability healing) *after* the turn's action has already resolved — whether that action was a landed attack, an attack blocked by sleep/paralysis/freeze, or a standby. This is why a poisoned Pokémon at 1 HP still gets to swing before it faints, instead of dying before its move — matches mainline Pokémon timing, was a deliberate 2026-07-03 fix per user report ("中毒的寶可夢還沒攻擊就倒下了").
- **Non-attack duration tick** (`tickNonAttackStatus()` single-player / `tickNonAttackStatusSrv()` PvP): decrements sleep/freeze/confusion `turnsLeft` on a turn where the Pokémon didn't attempt to attack (standby, or single-player's CPU-can't-afford-any-move branch) — no blocking, no confusion self-hit, just the countdown (and wake-up message) so standby genuinely progresses toward recovery instead of leaving the counter frozen forever.

Every turn-ending call site needs to call the right combination of these — grep existing call sites in `playerAttack`/`cpuAttack`/`playerStandby` (single-player) and the `attack`/`standby` WS handlers (PvP) before adding a new one. The trickiest wrinkle: applying poison/burn *after* an attack can newly kill an attacker that survived the attack exchange itself — in PvP this is handled by applying the tick and *then* computing `attackerDied`, so the existing double-KO/attacker-only/defender-only/neither branching (see KO resolution below) picks it up automatically; single-player does the equivalent via a shared `tickAndHandoff`/`tickAndEndTurn` closure that wraps whichever path the turn took.

CPU will self-cure **any** bad status (including freeze/sleep) with 萬能藥/治療師 if it has one in hand, before even attempting to attack — this is intentional pre-existing AI behavior, not a bug, even though it can look like "the status did nothing." Confirmed with the user this should stay as-is (2026-07-02).

## Damage calc & buffs — `doAttack()`

Buff object `{atkBonus, atkMult, shield, typeOverride, reflect}`, one per side (`G.playerBuff`/`G.cpuBuff` or `G[role+'Buff']`). All four of atkBonus/atkMult/typeOverride/shield reset the instant an attack actually happens (consumed regardless of hit/miss). `atkMult` used to only ever go *up* (trainer-card boosts, via `Math.max(buff.atkMult, N)`) until `intimidate` started pushing it *down* (`Math.min(opBuff.atkMult, 0.5)`, 2026-07-04) — same "resolve to the strongest applicable value in that direction" pattern, just mirrored for a debuff; a value below 1 in the UI's buff-tag badge now needs its own visual treatment (`.buff-tag.debuff`, red) distinct from the existing boost styling (`.buff-tag.mult`, orange) so a player can tell at a glance whether their next hit is buffed or debuffed. `reflect` is different — it's meant to persist until the *next* attack lands on that side, however many turns that takes, EXCEPT it must expire if the opponent's turn passes without attacking (switch, standby, or a status-skip) — every one of those turn-ending paths needs an explicit `xBuff.reflect = false`; there is no central place this happens automatically, so if you add a new way for a turn to end without an attack, check whether reflect needs clearing there too (grep `reflect expires` for the existing sites, single-player and server.js each have ~4).

Ability hooks are threaded through `doAttack` two ways — pick whichever fits the effect (see pokemon-data skill for the full roster of which ability uses which):
- **Post-hit** (`triggerAttackerAbility`/`triggerDefenderAbility` single-player, `*Srv` PvP): fired *after* damage is already applied, for effects that don't change the damage number itself (静電/static, 毒刺/poison-point inflicting a status; 粗糙皮膚/rough-skin dealing separate recoil). Simplest, least risky to add.
- **In-formula** (added 2026-07-03 for 9 new abilities): `abilityDmgMult` (attacker-side: 堅韌/guts, 大力士/huge-power, 激流-family/blaze-boost), `stabMult` (適應力/adaptability bumps it from 1.5→2), and a `defAbilityMult` product (厚脂肪/thick-fat, 硬岩/solid-rock, 神秘之守/frisk-ward) are all computed *before* the `dmg =` line and multiplied directly into the formula. Two abilities don't fit even that: `water-absorb` (儲水) is an early-return branch right after the `reflect` check — full immunity, heals instead, never reaches the damage formula at all — and `sturdy` (頑強) captures `wasFullHp = defender.cur === defender.hp` *before* the formula runs, then clamps `defender.cur` to 1 right after it's decremented, but only if that pre-hit snapshot was true. If you add another ability that needs to know pre-hit state, follow the `wasFullHp` pattern — capture the snapshot before any mutation, not after.

`triggerOnEnter`/`triggerOnEnterSrv` fire wherever a Pokémon becomes newly active (battle start, switch, KO-switch) — grep call sites before assuming you've covered every "a Pokémon just entered battle" moment, there are ~4-5 distinct call sites per engine. `intimidate` lives here: sets `opBuff.atkMult = Math.min(opBuff.atkMult, 0.5)` (changed 2026-07-04 from a flat `atkBonus -= 15`, deemed too weak — note the `Math.min` rather than straight multiplication, so it can't stack below ×0.5 or interact multiplicatively with an existing boost). It used to also check whether the opponent's active had `clear-body` before applying the debuff — that ability-vs-ability interaction pattern was deleted along with `clear-body`'s retirement the same day; if a future ability needs to veto another Pokémon's onEnter effect, the pattern (check the *opponent's* current active's ability inside the triggering ability's own code, not a passive block registered on the other side) still applies, just nothing currently uses it.

**Stale `ability.desc` text, fixed 2026-07-13**: the 2026-07-04 `atkBonus -= 15` → `×0.5` logic change updated the *behavior* everywhere, but only some of the 8 intimidate-carrying Pokémon's data entries got their `ability.desc` string updated to match at the time (大嘴娃/雷電獸/暴飛龍/頭巾混混 said "×0.5"; 暴鯉龍/穿山王/風速狗/布魯皇/肯泰羅/阿柏怪/流氓鱷/姆克鷹 still said the old "威力 −15"). The logic was never wrong — this was purely a cosmetic desc-string drift across all 3 engine files (`pokemon_battle.html`, `public/single.html`, `server.js`), each Pokémon's `ability.desc` is a hand-written literal, not derived from the trigger code, so a behavior change doesn't propagate to it automatically. **Lesson: when changing an ability's *effect*, grep every `ability:{id:'that-id', ...}` data-entry occurrence across all 3 files and check `desc` text matches the new behavior** — don't assume updating the one `triggerOnEnter`-adjacent line that computes the effect is sufficient, the flavor-text copy is a separate, easy-to-miss source of truth that has to be kept in sync by hand.

- **Status-infliction-time** (added 2026-07-04): `tryInflictStatus()` single-player / the inline status block inside `doAttack` in server.js — this is where a defender's ability can veto an incoming status (`own-tempo` blocks confusion, `insomnia` blocks sleep — same pattern, just a different `effect` string check) or piggyback on a successful one (`sync-status` copies poison/paralysis/burn onto the attacker after it lands on the defender). Any *card* that inflicts a status by directly setting `.status = {...}` instead of going through this function (混亂藥/confuse-potion, 瓦斯攻擊/gas-attack, 火焰彈/fire-bomb, 絕對零度/absolute-zero) bypasses these checks entirely — if a new status-blocking ability needs to also cover a specific card, add the same guard to that card's `applyTrainer` case individually (already done for `own-tempo` + confuse-potion, not yet done for the others — no Pokémon needs it yet).
- **Type-immunity-with-a-payoff** (the `water-absorb` early-return pattern, extended 2026-07-04): `motor-drive` (immune to electric, +3 energy to the defender's side instead of a heal) and `flash-fire` (immune to fire, `dBuff.atkBonus += 20` instead of a heal) both copy `water-absorb`'s early-return-right-after-the-`reflect`-check shape but swap out the payoff. If you add another type-immunity ability, this is the template: early return, no damage, `return`/`return { damage: 0, mult: 1 }` before the normal formula runs, and don't forget to still reset `aBuff`'s consumed fields (server.js does; single-player's older `water-absorb` block doesn't bother — a pre-existing minor inconsistency between engines, not something introduced 2026-07-04, low-risk since it only matters if the attacker had a leftover buff queued right before hitting an immune target).

Multiple Pokémon commonly share one `ability.id` **and can have different `name`/`desc` text per entry** — the dispatch is by id (`if (attacker.ability?.id === 'guts')`), not hardcoded per species or display name, so giving two different Pokémon the same ability (even under different in-game names, e.g. Overgrow/Torrent/Blaze all share `blaze-boost`) is the normal case, not a special one. When a real ability doesn't map to any existing effect and isn't worth a bespoke new one, reusing the closest existing id with a relabeled `name`/`desc` (see pokemon-data skill's ability table for examples like 超幸運→`huge-power`, 隔音→`frisk-ward`) is an accepted, deliberate shortcut — not a bug.

## Energy system (2026-07-02)

Every side has energy (`G.playerEnergy`/`G.cpuEnergy` single-player, `G.p1Energy`/`G.p2Energy` PvP), 0-20, starts at 5, `+3` capped at 20 at the start of *that side's own turn* — single-player hooks this into the exact same two spots that already fire once per turn-start (`cpuAttack()`'s top for CPU, `drawAfterTurn()` for the player); PvP hooks it into `drawForRole(G, role)`, which is already the single canonical "this role's turn is genuinely starting" function (correctly handles the KO-switch-queue-deferred case too — see pvp-networking skill). Resets to 5 on a new single-player stage / new PvP game, same place HP gets fully healed.

Each move's `cost` (see pokemon-data skill) is deducted right before `doAttack` actually fires — never on a status-skip. **PvP validates energy server-side** in the `attack` WS handler (`if (G[role+'Energy'] < atkCost) reject`) before trusting `msg.idx` — client-side button disabling is a UX nicety only, not the real guard. CPU (single-player only) filters its move choices down to `cost <= G.cpuEnergy` before applying its usual damage-preference logic; if literally nothing is affordable it skips the attack via the same `onSkip`/`endTurn` path status-skips already use. In practice this branch rarely fires — every tier's weak-move cost ceiling is ≤ the flat +3 regen, so some move is affordable right after any turn-start regen by construction.

**`effectiveCost(atk, opponentPoke)` single-player / `effectiveCostSrv(atk, opponentPoke, G)` server.js / `pvp.html`'s own client-display copy** is the one place a move's *effective* cost can differ from its base `cost` — currently only the 海洋世界 stadium's water-cost-halving. **Every place that checks or deducts a move's cost must go through this helper, not `atk.cost` directly** — `playerAttack`/`cpuAttack`'s affordability checks and deductions, `renderActions()`'s button-disable + displayed number, PvP's `attack` WS handler (validation *and* deduction — both use the same computed `atkCost` local so they can't drift apart), and `pvp.html`'s own `effectiveCost`/`renderActions()`/`playerAttack()`. If a future ability or card needs to affect energy costs, extend this helper rather than duplicating the tax logic at each call site.

**壓迫感/`pressure`'s continuous +2 tax — added 2026-07-04, removed 2026-07-14, not just disabled.** For 10 days this helper also added `Math.min(20, atk.cost + 2)` whenever the opponent's active had `pressure` (on top of whatever `pressure`'s own onEnter-drain already did — see pokemon-data skill). The user asked for the continuous tax removed while keeping the onEnter drain (a mix-up with `intimidate` while reviewing the ability's real effect surfaced this) — all 3 copies of the helper had the `opponentPoke?.ability?.id === 'pressure'` branch deleted outright (not commented out, not gated behind a flag), so `pressure` is back to being a pure onEnter-only ability. If similar "opponent's move costs more while I'm the active defender" effects come up again, this needs new code, not an old switch to flip back on.

Energy-granting cards: 3 items (`energy-patch-s/m/l`, +2/+3/+4) and 1 supporter (`cheerleader`, fills to 20) — same `applyTrainer` switch as everything else. The discard-2-for-1 trade modal (`chooseTrade()`/PvP `discard_trade`) has a third option beyond stadium/item: `cardType:'energy'` grants +5 instead of drawing a card.

## Mega Evolution (2026-07-04, energy model reworked same day)

**The Mega gauge is a per-side shared resource, not per-Pokémon** (reworked from an initial per-instance design the same day the feature shipped, per explicit user request: "讓mega 條跟能量條一樣，所有寶可夢共用"). Lives on `G` exactly like move-energy: `G.playerMegaEnergy`/`G.cpuMegaEnergy` (0-20) single-player, `G.p1MegaEnergy`/`G.p2MegaEnergy` PvP, plus a one-time-use gate `G.playerMegaUsed`/`G.cpuMegaUsed` (`p1MegaUsed`/`p2MegaUsed` PvP). The battle-instance clone itself only keeps `poke.megaEvolved` (bool, `undefined` if the species has no `mega` data) — there is no more per-Pokémon `megaEnergy` field. Only 23/100 Pokémon have `mega` data (see pokemon-data skill); everything below no-ops for the rest.

**One mega evolution per side per battle, full stop.** Once `G.xMegaUsed` is set, the gauge is gone for the rest of that battle even if the team has other Mega-capable Pokémon on the bench and even if the gauge could theoretically refill — there is no per-Pokémon accounting to get around this, `xMegaUsed` is the single source of truth checked everywhere (accumulation, the evolve button, the WS handler, the UI's show/hide).

**Accumulation** — inside `doAttack`, right after damage is computed and applied (both engines): `megaGain = Math.max(1, Math.round(damage / 25))`, added to **only the attacker's** gauge, gated by `!G[aSide+'MegaUsed']` — **not** by whether the attacking Pokémon currently on the field has `.mega`. The gauge charges from combat regardless of who's fighting, same as move-energy; a team of three non-Mega Pokémon technically still charges a gauge, it's just going to be moot since no active Pokémon can ever be eligible to spend it. Single-player determines the attacker's role via the `aSide` string already passed into `doAttack`; server.js has no such string, so it back-derives it by identity-comparing `aBuff` against `G.p1Buff` (same trick already used for `motor-drive`'s energy refund). **The defender does not gain anything from taking damage** — an earlier version of this feature credited both attacker and defender identically from every exchange, which the user found made the two sides' gauges look linked/synced in practice (they moved in near-perfect lockstep turn after turn, even though they were genuinely separate variables) — changed 2026-07-05 to attacker-only per explicit request, so each side's gauge now visibly reflects "damage *I* dealt," not "damage anyone dealt near me."

**Triggering** — `applyMegaEvolution(poke, side)` single-player / the inline `mega_evolve` WS handler in server.js: mutates `poke.id` (→ `mega.spriteId`), `poke.type`/`type2`, and `poke.ability` (→ a full copy of `mega.ability`), sets `megaEvolved = true`, **bumps `poke.hp` by ×1.2 (rounded) then fully heals `poke.cur = poke.hp` and clears `poke.status = null`**, sets the side's `MegaUsed` flag, then calls `triggerOnEnter(poke, side)` so 複製/`trace` can fire immediately, then kicks off the evolution animation (see below). **This is a free action — it must never end the turn**, same as before the rework. Gate conditions (duplicated in the UI's button-disable, same "client is a UX nicety, server is the real guard" pattern as move-energy): `poke.mega && !poke.megaEvolved && !G[side+'MegaUsed'] && G[side+'MegaEnergy'] >= 20`; PvP additionally requires `G.turn === role && !G.pendingKOSwitch`.

**Universal Mega power boost** (added 2026-07-05, after user feedback that Mega Evolution "didn't feel much stronger" — many of the reused abilities across the 87 Mega-capable species are situational/defensive rather than reliably impactful, e.g. `frisk-ward`'s 25% proc or `solid-rock`'s supereffective-only reduction, so a lot of evolutions changed little turn-to-turn). Two flat multipliers apply on top of whatever the species' specific Mega ability does, independent of `ability.id`: **+20% max HP** (the `poke.hp *= 1.2` bump above, applied once at evolve time — a real HP increase, not a shield or heal-only effect, so it also raises how much damage the Pokémon can soak afterward) and **+10% flat attack damage**, via `megaBoostMult = attacker.megaEvolved ? 1.1 : 1` multiplied into the final `dmg`/`damage` calculation in `doAttack` (both engines), stacked alongside `abilityDmgMult`/`defAbilityMult` rather than folded into either — keeps it simple to reason about ("Mega always adds +10% on top, no matter what the ability does") and means it doesn't need touching if a species' Mega ability is later redesigned. Deliberately *not* implemented as a per-species tunable, to keep all 87 Mega-capable Pokémon consistent — if a specific species still feels weak after this, prefer adjusting its Mega `ability`'s own effect over adding a per-species exception to this multiplier.

**⚠️ Superseded 2026-07-22, then again 2026-07-31 — the `megaBoostMult`/`×1.1` described above is no longer what's in the code.** The 2026-07-22 flat-bonus-conversion pass (see that section below) turned it into `megaBoostBonus = attacker.megaEvolved ? 40 : 0`, a flat additive term instead of a multiplier — the +20% HP bump is untouched, only the damage half changed shape. **Current (2026-07-31) three-way version**, after the user asked for the whole mega-capable/non-mega-capable damage curve to be explicitly ordered (non-mega > mega-pre-evolution, mega-post-evolution > non-mega): `const megaBoostBonus = !attacker.mega ? 40 : attacker.megaEvolved ? 100 : -20;` (`pokemon_battle.html`/`public/single.html`/`server.js`, identical expression in all three) — species with no `mega` field get +40 (same unit as everything else in this formula), Mega-capable species not yet evolved get **-20** (a real penalty, not just "no bonus"), and a Mega-evolved attacker gets **+100** (up from 40), so the three states now sit in a clean `-20 < 40 < 100` order matching the user's spec exactly. Additionally, **Mega-evolved attackers now pay 20% less energy on every move**: `effectiveCost`/`effectiveCostSrv`/`public/pvp.html`'s own client-display copy (see the "three-copy pair" lesson above — all three needed the edit) each gained `if (attackerPoke?.megaEvolved) cost = Math.floor(cost * 0.8);` right after `let cost = atk.cost`, stacking multiplicatively with the pre-existing Ocean World/`costHalved` discounts the same way those already stack with each other. No per-species tuning — same "keep all Mega-capable Pokémon consistent" philosophy as the original 2026-07-05 boost, just three flat states instead of one. Verified via live browser injection of `doAttack()`/`effectiveCost()` with a synthetic neutral-type move (`dmg:100, cost:10`) across all three attacker states, single-player engine: non-mega → 140 dmg / 10 cost, mega-pre → 80 dmg / 10 cost, mega-post → 200 dmg / 8 cost — exactly the expected `atk.dmg + megaBoostBonus` (no other multipliers active in the test) and `floor(10*0.8)`. `server.js`/`public/pvp.html` were verified by code-identity (byte-for-byte same ternary/multiplication line, confirmed via grep across all three files) rather than a live PvP match — proportionate for a single-value change to an already-existing additive term/existing helper, not a new mechanic requiring its own integration test.

**Persistence** — a specific Pokémon's evolved state still sticks to that battle-instance object for the rest of the battle regardless of switching (unchanged from the original design) — but since the whole *team* only gets one use, at most one Pokémon per side will ever carry `megaEvolved: true` in a given battle.

**UI visibility** — the Mega bar shows per side whenever `!G[side+'MegaUsed'] && deck.some(p => p.mega)` (checks the *whole roster*, not just the active Pokémon) — `renderSide(side, poke, deck, activeIdx)` already receives `deck` for this. The evolve button additionally requires the *currently active* Pokémon to itself have `.mega` (you can only evolve who's currently out).

**Mega Prism Tower** (`stadium-mega-prism`, added same day): grants **+10 Mega energy to whichever side's own turn is starting**, hooked into the exact same two turn-start points the +3 move-energy regen already uses (single-player: `cpuAttack()`'s top for CPU, `drawAfterTurn()` for the player; PvP: `drawForRole(G, role)`) — gated on `!G[role+'MegaUsed']` so it stops mattering once a side has used its one evolution. Like all other stadiums in this game, it's a global rule both sides benefit from symmetrically, not tied to whoever played the card.

**Evolution animation — full-screen cutscene** (redesigned 2026-07-05, replacing an earlier in-card version; see the retired-approach note below for why): `playMegaEvolveAnim(side, poke)` single-player / `playMegaEvolveAnim(side)` PvP (PvP derives `poke` internally via `side === 'player' ? P() : CP()` — the state is already applied via `applyServerState()` by the time it runs). Both open `#mega-cutscene-modal`, a `position:fixed;inset:0` full-screen takeover (same show/hide-via-`.open`-class pattern as every other modal in this game, but at a higher `z-index:200` so it sits above everything including other modals) containing three elements inside `.mega-cs-stage`: `#mega-cs-stone` (a 💎 emoji that scales/rotates in, holds a pulsing glow, then "shatters" — scales up + flashes white + fades), `#mega-cs-sprite` (a dedicated `<img>`, **not** the battle card's `#player-sprite`/`#cpu-sprite` — src is set to `spriteUrl(poke.id)` only at the shatter moment, then pops in with `.reveal`), and `#mega-cs-title` (fades in after the sprite reveal, showing "`{name}` Mega 進化！"). Timeline (both engines identical): open+stone-appear at t=0, stone starts pulsing at 600ms, shatter+16-particle-burst+sprite-reveal at 1300ms, title at 2000ms, modal closes at 3200ms — after which the ordinary battle screen underneath (already fully updated, since `applyMegaEvolution`/`applyServerState` ran before this was called) is simply what's left. No `G.busy` handling needed to block input during the cutscene — the fixed-position overlay already intercepts all clicks by sitting on top of the DOM.

**Why not animate the in-battle sprite directly (retired approach)**: the first version wrapped/burst *on* the `.b-card` element using the actual `#player-sprite`/`#cpu-sprite` `<img>`, and reused that image for both the small in-card effect and the underlying idle/breathing animation. That coupling caused a real bug (`el.style.animation` inline styles silently suppressing `setSprite()`'s own `.sprite-enter` class-animation, whose `animationend` then never fired, permanently blocking `syncSpriteAnim()` from ever re-adding `sprite-idle` — reported by the user as "mega 進化的寶可夢不會動"). Using a **separate, dedicated `<img>` inside the cutscene modal** sidesteps this category of bug entirely — the cutscene never touches `#player-sprite`/`#cpu-sprite`'s classes, so it can't collide with `setSprite`/`syncSpriteAnim`'s lifecycle no matter how its own animation is implemented. If you build another full-screen or otherwise-isolated Pokémon-image effect in the future, prefer a dedicated image element over reusing the battle card's own sprite — it removes a whole class of animation-state bugs, not just the inline-style one.

**CPU AI** (single-player only, no PvP bot): `cpuAttack()`'s very first lines check `CP().mega && !CP().megaEvolved && !G.cpuMegaUsed && G.cpuMegaEnergy >= 20` and auto-apply before any move-selection logic runs — greedy/immediate, no timing strategy, matching this project's established "CPU AI stays simple" precedent (see 搏命/瘋狂博士 in the Trainer cards section above).

**4 new ability mechanics were needed** beyond simply flagging a mega-form's real ability (most Mega abilities reuse an existing effect id with a relabeled `name`, exactly like the legendary-weather-ability pattern — see pokemon-data skill's Abilities section for the full per-species table):
- `tough-claws`: flat `abilityDmgMult × 1.3` on every move (real Tough Claws only boosts contact moves; this game has no contact/non-contact move tagging, so it's simplified to apply universally).
- `technician`: `abilityDmgMult × 1.5` only when `atk.dmg <= 60` — maps cleanly onto this game's existing weak/strong move-power tiers (weak moves are always ≤48, strong always ≥65), so the real "boosts moves ≤60 power" rule needed zero reinterpretation.
- `mold-breaker`: attacker-side flag (`const moldBreaker = attacker.ability?.id === 'mold-breaker'`) that forces `thickFatMult`/`solidRockMult`/`friskWardMult`/`multiscaleMult` to `1` and skips the `sturdy` 1-HP save, regardless of the defender's actual ability — implemented by threading `!moldBreaker &&` into each of those four conditions plus the sturdy check, rather than a separate code path.
- `trace`: lives inside `triggerOnEnter`/`triggerOnEnterSrv` (not `doAttack`) — copies the opponent's *current* active ability (`{...opPoke.ability}`) onto self. Fires both on a normal switch-in and (via the explicit `triggerOnEnter` call inside `applyMegaEvolution`) at the instant of evolving, so Alakazam's Mega form immediately traces whatever the opponent has out at that moment rather than waiting for a future re-entry.

## KO / double-KO resolution — the trickiest part, read before touching

A Pokémon's death can come from three independent sources in the same exchange: (1) the direct attack damage to the defender, (2) reflect bouncing damage back onto the attacker instead, (3) a defend-ability's recoil (currently only 粗糙皮膚/rough-skin) hitting the attacker *in addition to* the defender taking normal damage. (1)+(2) can't both happen (reflect fully redirects, defender takes zero direct damage that exchange). (1)+(3) **can** both happen — a genuine simultaneous double-KO.

Both engines had (separately discovered, separately fixed 2026-07-02) bugs where checking only the attacker's death and returning early would silently skip ever checking whether the defender *also* died in the same exchange — PvP's version was worse, since it could leave the game state stuck (no winner declared, no forced switch) rather than just misattributing a winner. The fix pattern, if you touch this again: compute **both** `attackerDied`/`defenderDied` booleans up front, handle all 4 combinations explicitly (neither / attacker-only / defender-only / both), and for the both-died case check each side's *remaining team* alive-count independently before deciding win/loss/draw — don't just reuse the single-sided `handleKO`/`pendingKOSwitch` early-return logic, it assumes only one side could possibly be wiped. Single-player has no draw concept — any true double-team-wipe resolves as a player loss (`showResult('cpu')`), matching the precedent set by 搏命. PvP does support `G.winner = 'draw'`.

`bothTeamsWiped()` (single-player helper) and the inline `roleAlive`/`opAlive` checks (PvP) are the canonical way to ask "did both sides just run out of Pokémon" — reuse them rather than re-deriving.

**PvP-only bug, fixed 2026-07-08: `G.turn` was never transferred to the opponent when the attacker died without the defender dying** (reflect bouncing lethal damage back onto the attacker, or the attacker dying from its own confusion self-hit before ever landing a hit — `sResult.died`). Both branches correctly set `G.pendingKOSwitch = role` so the attacker picks a replacement, but neither set `G.turn = op` the way the sibling `defenderDied`/both-died branches right next to them already did — so once the attacker resolved their `ko_switch`, `G.turn` was still `role`, and `ko_switch`'s own `if (G.turn === role) drawForRole(G, role)` check let the *same* player draw and act again immediately, reported by the user as "反彈致死後應該換對方回合". Fix: both branches now also set `G.turn = op; G.round++; drawForRole(G, op);` right there (the opponent's own Pokémon is fine, so there's no reason to wait for a `ko_switch` round-trip before giving them their turn — contrast with the `defenderDied` branch, which correctly *does* wait, because in that case it's the opponent's own Pokémon that needs replacing first). The `sResult.died` branch additionally expires the opponent's `reflect` buff (`G[op+'Buff'].reflect = false`) — same "reflect expires if the attacker's turn ends without them actually attacking" rule already applied to the `sResult.skipped` branch just above it, since dying to your own confusion counts as never attacking. **Single-player never had this bug** — it chains callbacks synchronously (`handleKO('player', () => setTimeout(cpuAttack, 700))`) rather than checking a `G.turn` variable, so control flow can't get stuck on the wrong side the way a stateful turn-flag can. If you find a *third* "attacker dies without the defender dying" branch in the future, check it sets `G.turn = op` — this was the second time the same omission was found in sibling branches.

## Moveset overhaul: cost-tier system + 8 support moves (2026-07-14)

All 153 Pokémon's movesets were regenerated from scratch by a one-shot Node script (not hand-edited — see pokemon-data skill for the HP→tier distribution table and generation approach), replacing the old "2 weak + 2 strong (+ occasional mid-tier)" moveset shape with a 4-cost-tier system, and one of each Pokémon's 4 move slots is always overwritten with one of 8 new **support moves** (0-damage, pure-effect). This is the largest single data rewrite in the project's history (612 move entries across 2 engines) and introduced several genuinely new move-level fields and mechanics that didn't exist before.

**New per-move data fields** (all optional, `undefined` = doesn't apply):
- `megaBoost: true` — tier1 (cost 0-3) moves only. Doubles Mega-energy gain from that hit (`megaGain = Math.max(1, Math.round(damage/25)); if (atk.megaBoost) megaGain *= 2;` — both engines, right where mega energy already accumulates in `doAttack`).
- `bonusEnergy: N` — tier2/3 (cost 4-15) moves. Using the move sets `G[role+'BonusEnergyNextTurn'] += N`, consumed (added on top of the normal +3 regen, then zeroed) in `drawAfterTurn`/`drawForRole` — the exact same promote-then-consume pattern as 通訊封印's `SupporterLocked`→`SupporterLockedThisTurn`.
- `selfHeal: 0.15-0.30` — tier4 (cost 16-20) moves, roughly half of them (the other half get a `status` instead — see below). Heals `round((attacker.hp - attacker.cur) * selfHeal)` — a percentage of the attacker's *missing* HP, not of damage dealt — applied right after status infliction in `doAttack`, gated on `damage > 0`.
- `support: true` + `effect: 'brace'|'sword-dance'|'thief'|'shadow-dance'|'debuff'|'meditate'|'trick'|'focus-energy'` — marks a move as one of the 8 support moves (see below). These have **no `dmg` field at all** (not `dmg:0` — genuinely absent) — every UI spot that displays `a.dmg` needed a `a.support ? '輔助'/'威力'+a.dmg` guard (`renderActions`/`renderSelCard`/`showBenchPopup`/Pokédex in both engines, `renderActions`/team-select/bench-popup in pvp.html) or it prints `undefined`.

**Move-generation approach** (script, not committed to the repo — scratchpad only, same "one-shot conversion tool" precedent as the 2026-07-02 energy rebalance): for each Pokémon, an HP-bucket table decides how many of its 4 moves land in each cost tier (≤230 HP → 2×tier1+1×tier2+1×tier3+0×tier4; 231-270 → 1+2+1+0; 271-310 → 1+1+1+1; >310 → 0+1+1+2 — lower HP means more cheap/spammy moves, higher HP means more of the expensive top-tier). Within a tier, dmg/cost values are interpolated by each Pokémon's **HP percentile rank within its own bucket** (not by original move power, since the whole point this time was an HP-driven rebalance) plus a small ±12% jitter so same-HP Pokémon don't get numerically identical moves. The pre-existing move **names, types, and any already-set `status` field are preserved** for tier1-3 (only dmg/cost/megaBoost/bonusEnergy are new) — tier4 always gets a **fresh** status-or-selfHeal roll that replaces whatever status the original move had (never both at once — a move can have `status` XOR `selfHeal`, never neither-nor-both stacked). Exactly one of the 4 slots (chosen independently of tier, uniformly random) gets fully overwritten with a support move — this can and does sometimes overwrite a tier4/tier1/etc. slot, so **don't assume a Pokémon's tier-count distribution is fully visible in its final 4 moves** — one tier's worth silently became a support move instead for that specific Pokémon.

**The 8 support moves** — all execute through `executeSupportMove()` (`pokemon_battle.html`) / `executeSupportMoveSrv()` (`server.js`), a parallel dispatch path alongside `doAttack()` triggered when `atk.support` is true (checked at the exact call site that would otherwise call `doAttack`, in `playerAttack`/`cpuAttack` and the PvP `attack` handler) — energy is deducted and the turn ends exactly like a normal attack, just no damage formula runs:
- **撐住 (brace, cost 5)**: sets `G[side+'Braced']`. Checked in `doAttack` right after the `sturdy` ability check — but unlike `sturdy`, **works at any HP, not just full HP** (explicit user requirement, confirmed because the literal Chinese wording didn't require full HP the way the real ability does). Consumed by the very next hit taken (`damage > 0`), whether or not it actually saved the Pokémon.
- **劍舞 (sword-dance, cost 4)**: `aBuff.atkMult = Math.max(aBuff.atkMult, 1.5)` — reuses the exact existing buff mechanism/lifecycle as 全力出擊 (all-out-attack supporter card), so it needs zero new expiry code; persists until the Pokémon's own next attack lands, same as every other `atkMult` source in this game.
- **小偷 (thief, cost 5)**: byte-for-byte reuse of the existing 掠奪(plunder) trainer-card logic (random steal from opponent's hand, respects the 5-card discard cap) — just triggered from a moveslot instead of a hand card.
- **影舞 (shadow-dance, cost 5)**: sets `G[side+'CoinShield']`. Checked in `doAttack` right where `damage` is computed (before `sturdy`/`Braced`) — 50/50 coin flip, heads zeroes `damage` to 0 entirely (full negation, not a partial reduction like `shield`). Consumed by the next incoming attack regardless of the flip's outcome.
- **施加負面效果 (debuff, cost 3)**: the move's data already has `status:{effect, chance:1}` baked in at generation time (one specific status, randomly chosen per-Pokémon when the moveset was generated — not re-rolled at cast time) — 100% chance since there's no damage to trade off against. Single-player calls the existing `tryInflictStatus()` directly; server.js's version is a trimmed inline copy of `doAttack`'s `rollStatus` closure (that closure isn't a standalone function, so it couldn't be reused verbatim).
- **冥想 (meditate, cost 3)**: `G[side+'BonusItemDrawsNextTurn'] += 2` — consumed in `drawAfterTurn`/`drawForRole` by pushing 2 extra cards from the exact same `itemsOnly` pool already used for the normal per-turn draw.
- **詭計 (trick, cost 3)**: `G[side+'BonusSupporterDrawNextTurn'] = true` — consumed the same way, but draws from `TRAINERS.filter(c => c.cat === 'supporter')` (the same pool `standby` already uses). **This is a deliberate, explicit exception to the game's otherwise-universal "supporter cards only ever appear in the starting hand, never redrawn" rule** — confirmed with the user before implementing, since it's the one card in the game that can hand a player a *second* supporter mid-battle.
- **集氣 (focus-energy, cost 3)**: `G[side+'BonusEnergyNextTurn'] += 9` — same mechanism as tier2/3's `bonusEnergy` field, just a fixed +9 instead of move-derived.

**Reflect-style expiry, applied to `Braced` and `CoinShield`**: both are one-shot defensive flags meant to protect against the *opponent's next attack* — so exactly like `reflect`, they must be cleared if the opponent's turn ends *without* them actually attacking (switch, standby, or a status-skip), or they'd persist indefinitely across unrelated future turns. Added `G[op+'Braced'] = false; G[op+'CoinShield'] = false;` alongside every existing `G[op+'Buff'].reflect = false` site — single-player has 5 such sites, server.js has 4 (grep `reflect = false` in either file to find all of them; if you add a 6th/5th way a turn can end without attacking, it needs this triplet too, not just the reflect line).

**CPU AI**: `cpuAttack()`'s `affordable` move filter now also excludes `atk.support` moves entirely (`!cpu.attacks[i].support && ...`) — support moves need situational judgment ("should I brace now, or attack?") that doesn't fit the existing greedy damage-maximizing CPU logic, so CPU simply never uses any of the 8, same "complex-decision cards default to CPU-unused" precedent as 搏命/瘋狂博士. This is single-player only; PvP has no CPU AI to guard.

**Verification approach**: single-player mechanics were verified via direct `javascript_tool` injection (constructing synthetic Pokémon/attacks and calling `doAttack`/`executeSupportMove`/`drawAfterTurn` directly) — note that rapid-fire sequential calls without awaiting each one's internal `setTimeout` chains to fully resolve will cause state bleed between "iterations" that looks like a bug but isn't (observed firsthand: a selfHeal test showed the wrong healed value once because a previous test's queued animation callback fired mid-assertion — reloading the page between isolated tests avoids this). PvP mechanics were verified two ways: (1) extracting the actual `doAttack`/`executeSupportMoveSrv`/`drawForRole` source out of `server.js` via the same regex-extract-and-eval technique `scripts/verify.js` already uses for `POKEMON`/`TRAINERS`, then asserting exact outputs against hand-crafted Pokémon/moves (18 assertions, all passing against the real shipped code — remember to give the attacker and move *different* elemental types in tests that check exact damage numbers, or STAB silently multiplies your expected value by 1.5 and the test fails for the wrong reason); (2) one full real 2-tab WS-driven match confirming 劍舞's buff survives an intervening opponent turn and multiplies correctly into the next real attack's server-computed damage (12 base × 1.5 sword-dance × 1.5 STAB = 27, exactly matching).

## Second moveset rebalance pass (2026-07-15) — tier ranges/HP table changed, mechanics didn't

One day after the 2026-07-14 overhaul above, the user asked for a full re-rebalance with a new rule set — same underlying mechanics (`megaBoost`/`bonusEnergy`/`selfHeal`/`support`+`effect` fields, the `Braced`/`CoinShield`/`BonusEnergyNextTurn`/etc. G-flags, all 8 support move names) but **different numeric tiers, a different HP-bucket table, new support-move costs, and one brand-new rule** — regenerated with a second one-shot script, this time explicitly instructed by the user not to ask clarifying questions and to commit+push autonomously once done.

**New tier table (3 tiers now, not 4 — the old 16-20 cost tier4 was removed)**:

| Tier | Cost | Dmg | Extra effect |
|---|---|---|---|
| t1 | 0-5 | 10-40 | `megaBoost:true` **and** `bonusEnergy` (4-8) — both together now, not split across two tiers like before |
| t2 | 6-10 | 50-75 | `megaBoost:true` **and** `bonusEnergy` (4-8) — same as t1, just pricier |
| t3 | 11-15 | 90-120 | `status` XOR `selfHeal` (unchanged from before) |

**`bonusEnergy` is now derived from the move's own cost position within its tier, not from the Pokémon's HP percentile** — per the user's explicit "依據消耗" (based on cost) wording this time. `dmg`/`cost` themselves are still HP-percentile-driven exactly as before; only `bonusEnergy` changed its interpolation basis, computed as a second, independent lerp: `costPct = (cost - tierCostLo) / (tierCostHi - tierCostLo); bonusEnergy = lerp(bonusEnergyRange, costPct)`.

**New HP→tier-count table** (still 4 buckets, boundaries unchanged, only the counts per bucket changed since there are 3 tiers instead of 4 now):

| HP bucket | t1 | t2 | t3 |
|---|---|---|---|
| ≤230 | 2 | 1 | 1 |
| 231-270 | 1 | 2 | 1 |
| 271-310 | 1 | 1 | 2 |
| >310 | 0 | 1 | 3 |

**New rule: Pokémon with HP > 280 get zero support moves** (all 4 slots are damage moves) — previously every single Pokémon got exactly one. 280 sits inside the 271-310 tier-bucket but is a completely independent threshold with no relationship to the bucket boundaries — don't assume they need to line up. Roughly 94/153 Pokémon kept a support move, 59 lost it entirely.

**New support-move costs** (down from before): 撐住 5 (unchanged), 劍舞 4 (unchanged), 小偷 5 (unchanged), 影舞 5→**2**, 施加負面效果 3→**1**, 冥想/詭計/集氣 3 (unchanged).

**New rule: every support move grants a universal +5 bonus energy next turn, on top of whatever its own specific effect already does** — implemented as one extra line at the very end of `executeSupportMove`/`executeSupportMoveSrv` (after the `switch` block, unconditional): `G[side+'BonusEnergyNextTurn'] = (G[side+'BonusEnergyNextTurn']||0) + 5`. This **stacks additively** with 集氣's own `bonusEnergy:9`, so 集氣 now grants **+14** total, not +9 — `SUPPORT_EFFECT_ZH`'s display strings were updated accordingly in both engines (every entry now says "+下回合能量+5" except focus-energy, which just says the pre-combined "+14" directly rather than "+9（+下回合能量+5）" for readability). **If you add a 9th support move later, remember this universal +5 applies automatically since it's added after the switch, not per-case — you don't need to add it to the new case yourself, but you do need to know it's already there so you don't double-add it.**

**Regeneration methodology difference from the first pass**: rather than building on top of the already-rebalanced (2026-07-14) data — which would have compounded data loss, since that data already has one move slot per Pokémon overwritten with a support-move name/type instead of a real elemental attack name — the script pulled genuine pre-rebalance move flavor (names/types/original status chances) from git history (`git show <commit-before-2026-07-14>:server.js`), and re-applied it against the *current* HP/ability/type/mega fields (which reflect all the individual per-mon tweaks made since, e.g. the Xerneas HP fix). **If you ever need to regenerate movesets a third time, do the same** — reach back to the last commit with genuine hand-authored move flavor rather than the most recent rebalance's output, or you'll be re-randomizing on top of already-lossy data and the "preserve original move names" property silently degrades a little further with each pass.

Verified the same two ways as the first pass: a semantic Node validator asserting the new tier ranges/HP-bucket counts/support-cost table/>280-cutoff hold across all 153 entries, plus extracting `executeSupportMoveSrv`/`drawForRole` from the real `server.js` to assert the universal +5 stacks correctly with each of the 8 effects (9 assertions, all passing) and that `drawForRole` applies the combined total correctly. One live single-player browser check confirmed a HP-≤280 Pokémon's 集氣 move showing "+14" in the UI and setting `G.playerBonusEnergyNextTurn = 14` after actually being cast.

## Weak-multiplier-to-flat-bonus rebalance (2026-07-22)

User reported matches dragging on too long, suspecting weak buff cards were part of the cause. Root cause: an earlier "全面傷害倍率壓縮" pass compressed every card/ability damage multiplier by 80% (raw intended value squashed toward 1.0 via something equivalent to `1 + (raw-1)*0.2`), which had already been baked into the literal numbers in the code (not computed live via `compressMult()`) — so a card meant to swing a fight (e.g. originally ×1.4) had shipped as ×1.08, all but invisible. Flat `atkBonus` cards were never touched by that pass, so they stayed fully effective — meaning the only cards/abilities that actually mattered post-compression were the flat ones, and any buff-card draw with a %-based effect under ×1.1 was effectively a wasted card slot.

**Rule going forward: any self-boosting multiplier below ×1.1 should be a flat `+40` damage bonus instead, not a multiplier.** Multipliers at or above ×1.1 (STAB, blaze-boost, technician, adaptability, colosseum, ocean, gamble, etc.) stay as multipliers — this only applies to the "supposed to help but too weak to notice" band. Debuffs applied to the *opponent* (intimidate ×0.9, chance-debuff ×0.9, thick-fat/solid-rock/frisk-ward/multiscale's damage-taken reductions) are a different side of the formula (damage-received mitigation, not damage-dealt buff) and were explicitly excluded from this conversion — don't touch values below 1 that are reducing incoming damage, *unless* the user explicitly asks for that specific one (see rock-field below — this line originally said all stadium debuffs including rock-field were out of scope, but the user came back the same day and asked for rock-field specifically, so "out of scope" was this skill's default guess, not an immovable rule).

**Exception shipped later the same day: 岩石地帶/stadium-rock-field's damage-taken reduction, ×0.97→flat `-80`.** Despite being on the "debuff, not in scope" side of the line above, the user judged ×0.97 too weak to notice and asked for it specifically. Implemented by removing `rockFieldMult` from the `stadiumMult` multiplicative chain and adding a new `rockFieldReduction` flat term subtracted alongside `shieldTerm` at the very end of the damage formula (`... - shieldTerm - rockFieldReduction`, both engines) — same treatment as a one-shot card like iron-guard's "-70 damage" shield, just recomputed fresh every attack from the active stadium + defender type instead of being a consumed buff. **If another currently-untouched debuff (thick-fat, solid-rock, frisk-ward, multiscale, or any other stadium reduction) later gets the same complaint, this is the template**: pull it out of whichever multiplicative chain it's in, turn it into its own flat term, and subtract it in the same place `rockFieldReduction`/`shieldTerm` are subtracted — don't assume the "debuffs are out of scope" default still holds once a user asks about one by name.

**Mechanism**: `doAttack()`'s final formula used to be one big product: `(atk.dmg + aBuff.atkBonus + stadiumBonus + reversalBonus) * effectiveAtkMult * ... * abilityDmgMult * ... * megaBoostMult * stadiumMult * typeBoostMult`. Converting a multiplicative term to a flat bonus means moving it out of the multiplication chain and into the additive sum at the front, following the existing `stadiumBonus`/`reversalBonus` precedent (both already flat additions, added 2026-07-08ish for 特訓場/逆轉鬥技場). The current additive sum is `atk.dmg + aBuff.atkBonus + stadiumFlatBonus + legacyDmgBonus + abilityDmgBonus + megaBoostBonus + typeBoostBonus`, where `stadiumFlatBonus = stadiumBonus + reversalBonus + lavaBonus`. `abilityDmgMult` still exists as a separate multiplicative term for the abilities that stayed ≥1.1 (blaze-boost, technician) — **the ternary chain for abilities now has two separate variables, one flat (`abilityDmgBonus`) and one multiplicative (`abilityDmgMult`), split by whether that ability's value is above or below the ×1.1 line.** If you add a new ability with a damage multiplier, decide which chain it belongs in using that same threshold.

**`typeBoost` (used by 龍之波動/dragon-pulse and 順風/tailwind) needed a dual-mode object** since one stayed multiplicative (dragon-pulse, ×1.12) and one converted (tailwind, was ×1.04): `{ type, mult }` for the multiplicative case, `{ type, bonus }` for the flat case. `doAttack` checks both (`typeBoostMult`/`typeBoostBonus`), defaulting to the neutral value (1 or 0) for whichever field isn't present on that particular buff object. If you add a third type-conditional buff card, pick whichever field matches its intended strength — don't assume `typeBoost` is multiplier-only just because dragon-pulse was first.

**Converted this pass** (both engines + `desc:` text + rules-modal `<li>` text in `pokemon_battle.html`/`public/single.html`/`public/pvp.html`, verified via `grep -noE '×1\.0[2468]'` returning zero non-comment hits afterward): 捨身猛擊/focus-punch (×1.04→+40, self-damage 20% HP kept), 連擊/double-strike (×1.08→+40, `doubleStrike` status-reroll flag kept), 順風/tailwind (×1.04→+40, flying-only), 詭計/trick support move (×1.02→+40), and abilities 毅力‧guts/大力士‧huge-power/硬爪‧tough-claws/背水之刃‧desperate-blade/淬鍊之心‧status-immune-once/機械之心‧item-synergy/海洋支配‧drizzle-ocean/熔岩大地‧drought-lava/指揮‧legacy-boost (all ~×1.02–1.06 → +40), the generic Mega-evolution bonus (×1.02→+40), and 熔岩火山/stadium-lava's fire-type bonus (×1.02→+40; its water-type ×0.9 penalty is untouched, it's a debuff).

**Verification approach**: rather than a full integration test, isolated `doAttack()` in a live single-player browser session (`javascript_tool`) — forced a controlled attacker/defender pair (neutral types, huge HP pool to avoid KO/clamping noise), called `doAttack()` directly with a synthetic 100-power move, and diffed defender HP before/after. Confirmed: baseline 100 dmg with no buffs; +40 exactly with an ability, the Mega bonus, the legacy-boost buff, or a `typeBoost.bonus` all in isolation; an unconverted ability (blaze-boost ×1.1) still stacks multiplicatively with STAB as before (100 → 121, matching 100×1.1×1.1 exactly); the unconverted dragon-pulse `typeBoost.mult` path still works (100×1.12=112); and calling `applyTrainer()` for focus-punch end-to-end sets `atkBonus` to 40 (not `atkMult`) while its 20%-self-damage side effect still fires correctly.

## Hand-manipulation card cap, stadium draw weight fix, discard-trade redesign (2026-07-22, same day as the flat-bonus rebalance)

**Draw/steal cards capped at once per turn**: 隨機應變/quick-thinking, 換氣追擊/normal-refresh, 群聚共鳴/bug-swarm (all draw a card into your own hand) and 掠奪/plunder, 夜襲/night-raid, 暗夜掠奪/dark-heist (all steal a card from the opponent's hand into yours) are `cat:'item'` cards, which are normally unlimited-per-turn. They were strong enough to chain-snowball a hand in one turn, so they now share a single per-turn lock — `HAND_MANIPULATION_CARDS` (array of the 6 ids, defined once near `TRAINERS`) is checked against a new flag pair mirroring the existing supporter-card lockout exactly: `playerHandCardUsed`/`cpuHandCardUsed` in single-player, `p1HandCardUsed`/`p2HandCardUsed` in `server.js`. **If you add a 7th hand-manipulation-style card, add its id to `HAND_MANIPULATION_CARDS` in all three files (`pokemon_battle.html`, `server.js`, `public/pvp.html` — the last one only needs the id list for its own disabled-button UI check, not full game logic) — it automatically gets the shared lock, no new reset-site plumbing needed.** The reset sites are copy-pasted 1:1 next to every existing `SuppUsed = false` reset (grep `SuppUsed` to find all of them in a given file before assuming you've covered every "turn actually ends" moment — there are 4 in single-player, 4 in server.js). Cards NOT in scope: 手牌破壞/hand-wreck and 精神干擾/psychic-disrupt (opponent discards, doesn't come to your hand — pure disruption, not "loot"), and 幽冥追跡/ghost-drain (energy drain + discard, same reasoning).

**Stadium draw-weight gap fixed**: `getDrawPool(type1, type2)` already correctly filtered both item AND stadium cards by type (`!c.type || c.type === type1 || c.type === type2`), but `weightedPick()`'s `weight ?? 1` fallback meant type-tagged stadium cards (熔岩火山/stadium-lava, 海洋世界/stadium-ocean, 羅馬鬥技場/stadium-colosseum, etc. — 10 total) never actually got the `weight:10` that type-tagged item cards have always had. Mechanically reachable ("gated the same way") ≠ "drawn at the same rate" — the pool-membership filter and the weight are two separate things, and only the filter was ever checked/tested historically. All 10 now have `weight:10` explicitly set on their `TRAINERS` entries (both engines). If you add an 11th type-tagged stadium, don't forget the `weight:10` — the type-gating alone silently doesn't get you equal draw odds.

**Discard-trade mechanic redesigned** (`playerDiscardTrade`/`confirmDiscardTrade`/`chooseTrade` single-player, `pvpConfirmDiscardTrade`/`pvpChooseTrade` + server.js's `discard_trade` WS handler): was previously hardcoded to "discard exactly 2 → pick 1 of {stadium, item, +5 energy}". Now the player can discard 1 or 2, and the reward menu depends on how many: **discard 1 → {stadium card, +5 energy}; discard 2 → {item card, cure own status ailment}**. The two-tier structure (1-discard tier is the old "cheap/common" reward set, 2-discard tier is the old "item" reward plus a new cure option) is a deliberate value split, not arbitrary — if this gets tuned again, keep the "discard more = access to the stronger/more specific reward" shape. `_tradeDiscardCount` (module-level, both `pokemon_battle.html` and `public/pvp.html`) remembers which tier applies between the discard-selection modal and the reward-choice modal, since choosing the reward type happens as a separate modal/step after the cards are already gone from hand. **Server-side validates the discard-count/reward-type combination as an explicit allow-list** (`indices.length === 1 ? ['stadium','energy'] : ['item','cure']`) rather than trusting whatever `cardType` the client sends — this is the same "validate server-side, don't trust client-computed legality" pattern used everywhere else in this codebase (瘋狂博士's target validation, 屬性轉換's chosen type, etc.), just applied to a count-dependent allow-list instead of a single fixed check. The 'cure' branch reuses the exact same `if (active.status) { ...clear it... } else { ...no-op message... }` shape as the 萬能藥/antidote item card (`STATUS_EFFECT_ZH`/`STATUS_ZH` name lookup + null the `.status` field) — didn't invent a new status-clearing mechanism.

## Stadium buff pass, attribute-domain abilities, ability-seal + heal-seal cards (2026-07-22)

User asked for 4 things in one request: buff all 16 stadium cards significantly (make them "core strategy," not flavor), extend the drizzle-ocean/drought-lava "ability auto-switches stadium" pattern to the 8 remaining elemental stadiums (restricted to non-Mega-capable Pokémon), add a card that seals a target's ability, add a card that blocks a target's healing, and add a battle-screen button for the guide modal (previously start-screen-only). Confirmed via AskUserQuestion first: paralysis's "50% skip chance" was already 50% (the user actually meant the per-move infliction chance, but on clarifying picked the already-50% one — net result: no code change, just confirmed).

**16 stadiums, numbers roughly doubled** (see `TRAINERS` desc strings for the full new/old table). Two of them (evil-forest, shrine) were "forced-multiplier" cards where `effActive`/`srvEffActive` sets `m` to a fixed value before `compressMult()` runs — rather than adding a new flat-bonus term (this project's usual lever for buffing something under the ×1.1 compression line), it's simpler and reuses the exact same mechanism to just raise the target `m`: shrine's `m=2`→`m=5` (compressMult(5)=1.8), evil-forest's `m=2`→`m=4` (1.6). Two cards had no existing numeric lever at all (invert, dragon-valley) — those got genuinely new flat-bonus terms in `doAttack` (`invertBonus`: +25 when the post-invert multiplier is still >1; `dragonValleyBonus`: +35 flat for dragon-type moves), folded into the same `stadiumFlatBonus` sum as `stadiumBonus`/`reversalBonus`/`lavaBonus`. toxic-field got a genuinely new mechanic (poison DoT ×2 while active) — required adding a `G` parameter to `applyEndOfTurnStatusSrv` (it didn't have one before, same "helper missing a parameter it now needs" situation `effectiveCostSrv` hit for 海洋世界) and threading it through all 3 call sites.

**8 new "domain" abilities** (`dragon-domain`/`grass-domain`/`poison-domain`/`fighting-domain`/`psychic-domain`/`normal-domain`/`ground-domain`/`rock-domain`), one non-Mega Pokémon each, picked by programmatically filtering `POKEMON` for `!p.mega` + matching type (never guessed candidates from memory — see pokemon-data skill's naming-accuracy lesson, same discipline applied to ability-candidate selection). A single `DOMAIN_ABILITY_STADIUM` lookup (`{abilityId: {stadium, type}}`) replaces what would otherwise be 8 near-duplicate `if (poke.ability.id === 'x-domain')` blocks — one `if (DOMAIN_ABILITY_STADIUM[poke.ability.id])` in `triggerOnEnter`/`Srv` handles the stadium-switch, and one `DOMAIN_ABILITY_STADIUM[attacker.ability?.id]?.type === atkType` ternary branch in `doAttack`'s `abilityDmgBonus`/`abilityDmgBonus` chain handles the matching +40 flat bonus — both mirroring drizzle-ocean/drought-lava's existing shape exactly. `DOMAIN_ABILITY_STADIUM` is declared as a `const` *after* `doAttack` in source order in both files — this is fine and doesn't throw, since `doAttack`'s body only reads the identifier when actually *called* (well after the whole script has finished its top-level `const` initialization), not at parse time. Don't assume declaration order matters for anything that's only referenced inside a function body, only for top-level statements that execute immediately.

**封印特性/Ability Seal (2-turn, item card)** — the correctness-critical design choice: **don't** thread a boolean through every individual ability-id check the way `mold-breaker`'s existing `moldBreaker` flag does (that pattern only ever covered ~7 defender-side in-formula checks and was never meant to be a general "ignore this ability" switch). Instead:
- In `doAttack`, compute `attackerAbility`/`defenderAbility` **once**, near the top: `isAbilitySealedSrv(role, G) ? null : poke.ability`. Then every subsequent read inside that one function must go through the local, not `attacker.ability`/`defender.ability` directly. Enforced this by scripted substring replacement (`sed`) across just that function's line range after manually verifying the exact string `attacker.ability`/`defender.ability` doesn't appear as a substring of anything unrelated in that range — safe because `.ability` is a fairly specific property name in this codebase. This covers ~30+ read sites (STAB/adaptability, guts/huge-power/tough-claws/desperate-blade/blaze-boost/technician bonuses, thick-fat/solid-rock/frisk-ward/multiscale/sturdy/no-weakness-dodge/shield-invert/weaken-buffs defensive checks, water-absorb/motor-drive/flash-fire immunities, mold-breaker's own check, and the inline status-infliction block that's folded into `doAttack` server-side) in one pass, correctly, without touching every site by hand.
- `triggerOnEnter`/`Srv`, `triggerAttackerAbility`/`Srv`, `triggerDefenderAbility`/`Srv` each already had a single early-return guard (`if (!poke?.ability) return;` etc.) gating their *entire* body — extending that one line to also check `isAbilitySealed(side)` covers the whole function for free, no need to touch every internal `.ability.id` read inside them. Single-player's standalone `tryInflictStatus` got the same treatment via one added local (`targetAbility`) + a scoped replace of `target.ability?.id` → `targetAbility?.id` (4 sites, all within that one function — verified the exact string doesn't collide with `transformPokemon`'s unrelated `target.ability` a few hundred lines away, which uses no `?.id` suffix so the scoped string didn't match it).
- server.js's `triggerAttackerAbilitySrv`/`triggerDefenderAbilitySrv` didn't take `G` as a parameter (didn't need it before) — added it, one call site each. Also had to hoist `aRole`/`dRole`'s derivation (`aBuff === G.p1Buff ? ...`) from partway down `doAttack` to the very top of the function, because the water-absorb/motor-drive/flash-fire early-return immunity branches (which now also need to check `isAbilitySealedSrv`) fire *before* where those roles used to be derived — same "a helper needs data that isn't available yet at this call site" class of problem as `effectiveCostSrv` needing a late-added `G` param, just solved by reordering instead of adding a param.

**詛咒/Curse (2-turn, item card, blocks all healing)** — no single choke point exists for "this side is healing," so this one genuinely does need a guard at every heal site, enumerated once and reused: `potion-m/l/xl`+`revive`+`nurse` in `applyTrainer` (nurse is special — its status-cure half is NOT a "heal" per the user's scoping, so it still fires even when heal-sealed, only the HP-restore half is skipped), `poison-heal` ability (in `applyEndOfTurnStatus`/`Srv` — also needed a new `role` parameter threaded through 3 call sites since the function only received `poke`/`log` before), `water-absorb`'s heal-on-immunity branch (keeps the *immunity*, zeroes only the *heal* — same split as nurse), the `selfHeal` move field, the 羽棲/roost support move, and `stadium-spring`'s per-turn heal (loops both sides in one call — the guard goes *inside* the per-side loop body, not around the whole `if`, so only the sealed side gets skipped, mirroring the existing lesson about `applyEndOfTurnStatusSrv`-adjacent loops from the toxic-field pass above). Mega Evolution's and 瘋狂博士's full-heal-on-transform were deliberately left out of scope (identity-change side effect, not "a heal card/ability doing its normal job") — flagged in the plan as a boundary the user could ask to extend later, not implemented preemptively.

Both new seal cards reuse the **decrementing-turn-counter pattern** already established by 亡靈詛咒/`wraith-curse` and 暗影封鎖/`shadow-lockdown` for Mega-seals (`G[role+'AbilitySealedTurns'] = 2` / `HealSealedTurns`, decremented once at that role's own turn-start, right next to the existing `MegaSealedTurns--` line) — **not** 通訊封印's promote-then-consume pattern, which only ever locks the *immediately next* turn (1 turn). Don't conflate the two shapes: promote-then-consume is for "blocks exactly the opponent's next turn," the counter is for "blocks for N turns starting now."

**查閱效果 (guide modal) button**: the modal itself (`#guide-backdrop`, rules + Pokédex tabs) was already fully built and identical in both `pokemon_battle.html` and `public/pvp.html` — only its trigger button existed, and only on the start/lobby screen. Added a second trigger button inside `#battle-screen` calling the same existing `openGuide()`, no new modal content. Styling needed both a desktop rule (plain visible button in the `#log-drawer` sidebar, unlike `#log-drawer-btn` which is `display:none` by default and only shown via the mobile media query) and a mobile FAB rule added to the existing `@media(max-width:900px)` block — `pokemon_battle.html` only had 2 FAB slots taken (left/right bottom corners) so the new button went bottom-center; `public/pvp.html` already had a 3rd FAB (`#chat-btn`) stacked with `#hand-drawer-btn` on the right, so the new button was stacked above `#log-drawer-btn` on the left for symmetry instead of reusing the bottom-center slot — the two engines' mobile FAB layouts aren't identical, check what's already there before picking a position rather than copying pokemon_battle.html's choice blindly.

## 8 more stadium cards with genuinely distinct mechanics, domain abilities to 36 Pokémon, discard-trade fixes (2026-07-23)

Follow-up to the stadium buff pass above. User first asked for the 8 remaining typeless stadiums (electric/ice/dark/steel/flying/bug/ghost/fairy) — initially pitched as the same "flat bonus + type-counter multiplier" shape as the existing 16, but the user explicitly rejected that: "我想要除了單純傷害外，有另外的玩法，例如惡屬性是在場上對方不能回覆生命這種" (want an actual different mechanic per card, not just numbers — gave "dark = no one can heal" as the example). This is the reason each of the 8 new cards below pairs the established damage convention with one genuinely new subsystem hook rather than another multiplier term — **when a user says "not just numbers," look for an existing subsystem to reuse/extend rather than inventing a new one from scratch**, every mechanic below reuses something that already existed:

- **暗夜詛咒領域/stadium-dark-curse** (global no-heal): extended `isHealSealed(side)`/`isHealSealedSrv(role,G)` (the per-side counters from 詛咒/heal-seal, see the entry above) to also return `true` unconditionally when this stadium is active — every heal-gated call site from that pass (potion/revive/nurse, poison-heal, water-absorb, selfHeal, 羽棲/roost, stadium-spring) automatically inherited the new stadium-wide block for free, zero additional call-site edits needed. **This is the reusable lesson: if a new "global version" of an existing per-side toggle is needed, extend the toggle's own read function instead of touching every call site a second time.**
- **疾風之翼/stadium-flying-wind** (switching never ends the turn): reused the existing `usedFreeSwitch`/撤退背心 branch — `const usedFreeSwitch = G.freeSwitch || G.activeStadium?.id === 'stadium-flying-wind';` (single-player `executeSwitch()`) and the equivalent in server.js's `switch` handler. The existing `G.switchedThisTurn`/`G[role+'SwitchedThisTurn']` once-per-turn cap is a **separate** flag set unconditionally regardless of which branch fired, so it isn't bypassed by this — switching under this stadium is free but still capped at once per turn, matching what 撤退背心 already did. Had to capture the *original* trigger (`usedRetreatVest`) before overwriting `G.freeSwitch = false`, so the "(撤退背心：不消耗回合)" log message could correctly say "(疾風之翼：不消耗回合)" instead when that's what actually fired — don't reuse a flag for both "did this happen" and "why did this happen" without saving the original value first.
- **蟲群巢穴/stadium-bug-hive** (lifts the once-per-turn draw/steal-card cap): every `HAND_MANIPULATION_CARDS.includes(card.id) && G[side+'HandCardUsed']` gate (the useTrainer entry guard, the button-disabled check, `cpuUseTrainers()`'s check, PvP's client button check, PvP's server-side `use_trainer` validation — 5 sites total, all documented in the hand-card-cap entry above) got `&& G.activeStadium?.id !== 'stadium-bug-hive'` appended. The flag-*setting* sites were left untouched (harmless to still set a flag nothing reads while the stadium is up).
- **雷雲庇護所/永凍冰原** (ambient paralysis/freeze each turn-end): added right next to the existing `stadium-sandstorm` per-turn-loop block in `drawAfterTurn`/`drawForRole` (both engines) — same "both sides, one shared loop" shape, just rolls a status chance instead of a flat HP change. Guarded on `!poke.status` so it doesn't stack onto an existing status.
- **鋼鐵堡壘/stadium-steel-fortress** (flat incoming-damage reduction for *both* sides, not just steel/rock/ground like 岩石地帶): a new `steelFortressReduction` term subtracted at the very end of the damage formula alongside `rockFieldReduction`/`shieldTerm` — same subtraction site, just unconditional on defender type instead of gated.
- **亡靈墓園/stadium-ghost-curse** (blocks all status cures): gated `antidote`'s and `nurse`'s status-clear half (not the HP-heal half, which is a separate `isHealSealed` concern already) plus the discard-trade `cure` option — 3 sites, both engines. CPU AI's `useCard('antidote') || useCard('nurse')` needed no separate change since it routes through the same `applyTrainer()` cases.
- **妖精結界原野/stadium-fairy-ward** (-10% off every move's own status-chance roll, floor 0): single-player's `tryInflictStatus()` has one chance-roll site; server.js has **two** separate inline copies (doAttack's own status roll, and a second one inside `executeSupportMoveSrv` for the 施加負面效果/debuff support move's roll) — both needed the same `Math.max(0, atk.status.chance - 0.1)` treatment. `guaranteed`/`aBuff.guaranteedStatus` (心靈感應) bypasses the roll entirely and correctly stays unaffected by this reduction.

**Domain abilities expanded from 8 to 36** (2 per type × 18 types, including water/fire which now have a 4th and 2nd representative respectively on top of their pre-existing drizzle-ocean/drought-lava holders) — purely a `DOMAIN_ABILITY_STADIUM` table extension (8 new `{type}-domain` ids for the 8 new stadiums) plus reassigning 18 more Pokémon's `ability` field, chosen the same way as the first batch: programmatically filtered `!p.mega` candidates per type from the live `POKEMON` array, never guessed from memory. **Reused existing ids where the type already had one** (dragon-domain, drizzle-ocean, etc.) rather than inventing `-2` variants — multiple Pokémon sharing one ability id with different flavor names/mons was already the established norm before this session (`blaze-boost`, `pressure`, etc.), so this is consistent, not a new pattern.

**Guide-modal roster-list housekeeping**: reassigning an existing Pokémon's ability means it needs removing from its *old* ability's roster line, not just adding to the new one — missed once already in a prior session for `clear-body`'s retirement, same category of mistake here across 18 mons × up to 3 guide-text copies (`pokemon_battle.html`, synced to `single.html`, plus `public/pvp.html`'s own separate copy). While doing this pass, found and fixed a **pre-existing** stale entry unrelated to this session's changes: 固拉多 was still listed under 大力士/`huge-power`'s roster line despite having held `drought-lava` since a much earlier session — a leftover from whenever that reassignment first happened. **When touching a guide-text roster line for an unrelated edit, it's worth scanning the other names on that same line for staleness while you're there** — this one would likely have sat wrong indefinitely otherwise since nothing else would have surfaced it.

**Two independent discard-trade (棄牌兌換) bugs fixed, unrelated to the stadium cards**: (1) the stadium/item card drawn from `chooseTrade()`/server.js's `discard_trade` handler was never filtered by the active Pokémon's type — copy-pasted from before `getDrawPool()`-style filtering existed, just never updated when that convention was established elsewhere. Fixed by filtering `TRAINERS` the same way `getDrawPool()` does, both engines. (2) The discard-trade button's disabled condition checked `hand.length < 2` in both `pokemon_battle.html` and `public/pvp.html`, but the actual trade logic (`confirmDiscardTrade()`/the modal's own selection cap) has supported discarding just 1 card since the 2026-07-22 redesign — a stale threshold left over from the old "always discard exactly 2" design, not an intentional limit. Grep the *actual* validation logic before trusting a UI disabled-condition's threshold when a bug report says "X should be allowed but isn't" — the enable/disable check and the logic it's supposed to mirror can drift apart silently, especially after a redesign that changed the logic but missed one of its guards.

**New user-requested limit, not a bug**: discard-1-for-a-stadium-card capped at 3 per turn (the +5-energy alternative at the same discard-1 tier is unaffected) — a `G[side+'StadiumTradeCount']` counter incremented in `chooseTrade('stadium')`/server's `discard_trade` handler, reset at the same turn-start point as other per-turn counters (`playerSuppUsed`-adjacent in single-player's `endTurn()` closure, `drawForRole`'s top in server.js), checked both to disable the button pre-emptively and again inside the actual handler (defense-in-depth / server-authoritative validation, same "don't trust the UI alone" pattern used everywhere else in this codebase).

## Move riders (added 2026-07-27, not previously documented here) + damage-tier flavor pass (2026-07-31)

**⚠️ This skill had no coverage of the "rider" system before this entry — it shipped 2026-07-27 (a week after this doc's last update) directly replacing the 2026-07-14/07-15 support-move system.** As of 2026-07-31, `POKEMON.flatMap(p=>p.attacks).filter(a=>a.support).length === 0` — every move slot is a damage move now; the 8 support moves (`brace`/`sword-dance`/etc., see the 2026-07-14 section above) still have live dispatch code in `executeSupportMove`/`Srv` but no data currently sets `atk.support`, so that whole code path is dormant, not deleted. **`atk.rider`** is the replacement: a string field on ~260/608 move slots (mostly low-cost ones — see below), dispatched in a `switch(atk.rider)` block inside `doAttack` right after the existing on-hit hooks (`megaDraw`/`megaSeal`), gated on `dmg > 0`. 9 rider ids exist: `energy-steal`, `guard-up`, `card-steal`, `life-drain`, `weaken`, `self-cure`, `type-draw`, `move-reflect`, `mega-charge` — each engine has its own switch (`pokemon_battle.html`'s reads `aSide`/`dSide` strings, `server.js`'s reads `aRole`/`dRole`) plus a shared `RIDER_EFFECT_ZH` id→description map (`pokemon_battle.html` and `public/pvp.html` each keep their own copy for display; `server.js` has no display layer, just the log-message literal text baked into each case). **Distribution is already heavily cost-skewed**: at cost≤5 (t1) 243/283 moves (86%) carry a rider; cost 6-10 (t2) only 17/147 (12%); cost 11-15 (t3) **zero** — riders were designed as the low-cost tier's flavor, t3 had no equivalent identity before this session.

**2026-07-31 pass, user request: "低傷害的可以有比較強力的效果...傷害高的則多再打傷害...吸收對手能量的招式可以吸收更多一點"** — three independent changes, one value tweak + two brand-new move-level fields, computed by a one-shot Node script (scratchpad, not committed) and applied via **targeted text-surgery per move object** (not full-array re-serialization) — verified first that no `attacks:[...]` span in either engine contains `//`/`/*` comments or escaped quotes (both true, checked via a small Node scan), then used a string-aware (single-quote-respecting) brace-depth splitter to isolate each pokémon's 4 move-object substrings and append new fields before each one's closing `}`, preserving every untouched field/pokemon byte-for-byte. Computed once from `pokemon_battle.html`'s parsed data, applied identically to both engines by `(pokemon id, move index)` — `scripts/verify.js`'s existing "attacks/ability/mega data identical across both engines" check passed with zero mismatches afterward, confirming the two applications landed identically without needing a second independent computation.

- **`energy-steal` buffed**: 5 energy drained / 3 restored → **8 / 5**. Pure value edit, both engines' switch case + both `RIDER_EFFECT_ZH` copies + `server.js`'s log-text literal — no data-layer change needed since the amount lives in code, not per-move.
- **`atk.status2` — dual-status for low-cost moves**: every t1 move (cost≤5) that already had a `status` field got a second, different-effect `status2` (same `chance` as the first, effect deterministically picked from the remaining 5 status types via `(id*11 + moveIdx*5) % 5`) — 54 moves. Engine change: single-player added one line right after the existing `wasDoubleStrike` reroll — `if (dmg > 0 && atk.status2) tryInflictStatus({ status: atk.status2 }, defender, dSide, attacker, guaranteedStatusThisHit);` — reusing `tryInflictStatus`'s existing immunity/reflect/two-slot handling by passing a synthesized `{status: ...}` object, the exact same trick 冰霜咆哮's `iceHowlFreezeThisHit` line already used one line below it. `server.js` has no standalone `tryInflictStatus` (its status roll is an inline `rollStatus` closure inside `doAttack`, used by both the base roll and 連擊/double-strike's reroll) — generalized it to take an optional `statusOverride` param (`const st = statusOverride || atk.status;`, then every internal `atk.status`/`atk.status.chance` read inside the closure became `st`/`st.chance`), and added `if (atk.status2) rollStatus(atk.status2);` after the existing `rollStatus(); if (aBuff.doubleStrike) rollStatus();` pair. **A real bug was hit and fixed while implementing this, not just verifying it**: the first version's live-browser test showed `status2` never applying even though `status` (the first roll) worked — traced via `window.__debugTrace`/global-error-listener injection (console.log calls from `javascript_tool`-injected code turned out unreliable to observe via `read_console_messages` mid-session — a `window.addEventListener('error',...)` + pushing to a `window.__x` array was what actually surfaced it) to an **uncaught exception inside `tryInflictStatus`'s own `renderBattle()` call**: my synthetic test Pokémon objects lacked an `.attacks` array, so `renderActions()`'s `p.attacks.forEach(...)` threw partway through the first `tryInflictStatus` call, silently aborting the rest of that `setTimeout` callback (including the second, status2, call) — since the test's `await new Promise(resolve => {...; setTimeout(resolve, 1800);})` pattern doesn't reject on an in-callback exception, the test just "completed" 1800ms later with the tail half of the function never having run. **Lesson for future direct-injection testing of `doAttack`**: any synthetic Pokémon passed as the *battle-instance* attacker/defender (i.e., set into `G.playerDeck`/`G.cpuDeck`) needs a plausible `attacks` array even if the test never calls that Pokémon's own moves, because `tryInflictStatus`/other mid-`doAttack` calls trigger `renderBattle()`→`renderActions()`, which unconditionally reads `P().attacks`/`CP().attacks`. A bare `{name,type,hp,cur,ability,status}` object (sufficient for earlier single-value formula tests like the Mega rebalance above, since those only read `defender.cur` *before* any render call) silently breaks any test that needs code *after* the first status/ability trigger to run.
- **`atk.bonusVsType` — bonus damage for high-cost moves**: every t3 move (cost 11-15, 178 total, 170 got a value — 8 skipped because their move-type has no listed 2x row in `EFF`, e.g. some off-meta types) got `bonusVsType: <type>` set to the type that move's own `type` is 2x-effective against in the existing chart (deterministic tie-break via `(id*7 + moveIdx*3) % candidates.length` if multiple 2x targets exist) — i.e. every strong move's damage-tier flavor now reinforces its *own* type's real strength, not an arbitrary assignment. Engine: new flat term `bonusVsTypeBonus = (atk.bonusVsType && (defender.type === atk.bonusVsType || defender.type2 === atk.bonusVsType)) ? 50 : 0;`, folded into the same additive sum as `megaBoostBonus`/`typeBoostBonus`/etc. (both engines' `doAttack`, right before the multiplicative chain — see the Mega-rebalance section above for the exact formula this joins).
- **`atk.ignoreShield` — piercing flavor for ~70% of high-cost moves**: a brand-new move-level field (parallel to the pre-existing `atk.ignoreReflect`, which was previously only a scattered ~20%-of-moves flavor across all tiers, untouched by this pass) — `((id*7 + moveIdx*3) % 10) < 7` deterministically selects ~70% of t3 moves (126/178) to get `ignoreShield: true` **and** `ignoreReflect: true` (skipping `ignoreReflect` specifically if that move already had it from an earlier, unrelated data-authoring pass, to avoid a duplicate object key — the generation script explicitly checks `!a.ignoreReflect` before adding it, `ignoreShield` never pre-existed so it's unconditional). Engine: `shieldTerm` (both engines) changed from `aBuff.ignoreShield ? 0 : ...` to `(aBuff.ignoreShield || atk.ignoreShield) ? 0 : ...` — the move-level flag and the existing buff-level one (set by 直搗黃龍/pierce-shield card) are independent ORed conditions, either alone bypasses the defender's shield for that hit.
- **UI**: `showMoveInfo()` (the "點擊查看招式詳情" popup, both `pokemon_battle.html` and `public/pvp.html`'s copies — see the energy-system section's "3-copy" precedent, though this popup itself was always a 2-copy: single-player + PvP-client-display, `server.js` has no UI) gained rows for `a.status2` (mirrors the existing `a.status` row), `a.bonusVsType` (shows the type name + "+50 傷害"), and `a.ignoreShield` (mirrors the existing `a.ignoreReflect` row) — same `if (a.field) rows.push(...)` pattern as every other optional move attribute already there.

**Verification**: single-player tested via live `javascript_tool` injection of `doAttack()` with synthetic moves (6 scenarios: dual-status both landing, bonusVsType match/no-match, ignoreShield bypassing an active 50-shield vs. a control move that doesn't, buffed energy-steal's exact 8/5 numbers) — all matched expected values exactly once the `.attacks`-array test-harness bug above was fixed. `server.js` verified by **actually extracting and running** `doAttack`/`rollStatus`/`inflictStatus`/`srvEffActive`/`compressMult` out of the real file (a corrected version of the extract-and-eval technique `scripts/verify.js` uses — the first attempt at this in the same session, for the Mega rebalance above, was abandoned after a naive brace-counter got lost inside a `//`/`/*` comment and swallowed almost the whole file; this time the extractor properly tracks line-comment/block-comment/string state alongside brace depth) and asserting the identical 6 scenarios against it in a standalone Node script — all 6 produced numbers identical to the single-player browser results, confirming true parity rather than just code-identity. A real single-player battle was also started in-browser (team-select → battle screen) to confirm no rendering crash from the data changes, plus a direct `showMoveInfo()` call with a synthetic move carrying all 5 new/existing optional fields at once to visually confirm the popup lays them out correctly.

## Roster-wide dmg/cost fairness pass (2026-07-31, same day as the rider pass above)

User reported "some Pokémon have high damage for low cost, others low damage for high cost" — measured the actual damage-per-energy ratio across all 608 move slots and found it varied **5-7x within the same HP bucket** (e.g. ≤230 HP bucket ranged 10.63 to 54 damage-per-cost-point), caused by several historical passes stacking unevenly (the 2026-07-18 flat "+30 to every move's `dmg` regardless of cost" bump inflated cheap moves' ratio far more than expensive ones', since +30 on a cost-1 move roughly quadruples its ratio but barely moves a cost-15 move's). Also found the **HP-bucket → tier-count table** (2026-07-15 pass, ≤230→2×t1+1×t2+1×t3 etc.) had drifted for individual Pokémon after many subsequent per-card/move tweaks — e.g. 阿柏怪 (HP 200, should be in the 2/1/1 bucket) had ended up with 3 cost≤5 moves and zero cost≥11 moves.

**Fix, one-shot script (scratchpad, not committed) applied to both engines' `POKEMON` arrays**:
1. Re-derive each move's tier fresh from the canonical HP-bucket table instead of trusting current cost: rank the 4 move slots by **current `dmg` ascending** (same ordering principle `applyMegaMoveset` already uses — weakest slot stays weakest), then assign tiers by that HP bucket's t1/t2/t3 counts in rank order.
2. New cost, within the assigned tier's band (`t1:[0,5]`, `t2:[6,10]`, `t3:[11,15]`, unchanged bands): HP-percentile-within-bucket interpolation (same convention as prior passes) plus a small ±0.5 deterministic jitter.
3. New `dmg = round(max(newCost,1) × 14 × jitter)`, jitter ~0.94-1.06 (id/moveIdx-derived, deterministic). **`14` was chosen as the pre-existing overall weighted-average ratio** (13.83), specifically so total roster power stays roughly where it was — this pass fixes *fairness*, not overall strength.
4. Applied via the same string-aware move-object splitter as the rider pass above, but this time using a **regex replace of just `dmg:\d+`/`cost:\d+` within each move object's existing substring** (not appending new fields) — simpler than the rider pass's field-injection since these two keys are unambiguous within a flat move object (no nested field shares the name) and every move already has them.

**Mega postform got the identical treatment** — `applyMegaMoveset`'s `MEGA_MOVESET_BANDS` (a `dmgLo/dmgHi` range interpolated by rank fraction, which had the exact same "rank0 gets a much better ratio than rank3" problem: tier1 species' rank0 was ratio 16.25 vs rank3's 9.58) was replaced with `MEGA_MOVESET_RATIO = {1:16, 2:18, 3:20}` (keyed by species power-tier, unchanged `MEGA_MOVESET_COSTS=[4,6,9,12]`) and the derivation became `move.dmg = Math.round(cost * ratio * jitter)` — same "derive dmg from cost via a fixed ratio" principle as the base moveset fix, just with a tier-scaled premium ratio (16/18/20, all higher than the base formula's 14, as Mega's reward) instead of one flat 14. **This is unrelated to `megaBoostBonus`** (the flat +40/+100/-20 additive term applied at battle time based on mega-capability/evolution state, see the Mega Evolution section above) — that stays exactly as it was; this fix only touches how the *moveset itself* is generated at the moment of evolving. Every other per-move field `applyMegaMoveset` sets (`bonusEnergy`/`selfHeal`/`megaDraw`/`megaSeal`/status boost) is untouched, only the `dmg`/`cost` derivation lines changed.

**Scope discipline**: only `dmg`/`cost` fields were touched anywhere — move names/types/`status`/`status2`/`rider`/`megaBoost`/`bonusEnergy`/`selfHeal`/`ignoreReflect`/`ignoreShield`/`bonusVsType` (the last 3 added in the same-day rider pass above) all stayed byte-identical, verified by diffing the full 608-move JSON before/after outside the `dmg`/`cost` keys.

**A real accidental-write incident happened while iterating on the generation script**, worth remembering as a general caution: a throwaway Node wrapper used to inspect the script's internal `changes` object (by string-replacing the script's own `process.exit(0)` dry-run guard with a variable-capture line, then `require()`-ing the patched copy) inadvertently deleted the *only* thing preventing the script from falling through into its real `--write` file-writing section — since the guard's early-return was itself the thing being patched out, the script wrote to the real `pokemon_battle.html`/`server.js` despite `--write` never being passed. Caught immediately by an unplanned `git status`/`git diff --stat` right after, and the write turned out to exactly match the intended output (verified via ratio/parity checks) so it was kept rather than reverted. **Lesson: never string-patch out a script's own dry-run/write guard to add instrumentation — capture data through a side channel that can't also disable the safety check** (e.g. have the script itself accept a `--dump-json <path>` flag from the start, or wrap the require in a way that doesn't touch the guard's source at all).

**Verification**: overall ratio distribution recomputed after the write — 13.00-14.50 (was 6.25-54.00) across all 608 moves, weighted average 13.83 (unchanged from before, confirming no net power shift). `scripts/verify.js` parity check confirmed both engines' `attacks` arrays identical post-edit. Mega postform verified two ways matching the rider pass's precedent: live single-player `javascript_tool` call to the real `applyMegaMoveset()` against one Mega-capable Pokémon per species-tier (1/2/3), and a standalone extraction of `server.js`'s copy of the same function run against the same 3 species — both produced byte-identical dmg/cost arrays and per-move ratios clustered tightly around each tier's target (16/18/20).

## Move-effect count equalized across the roster (2026-08-01, same request as the ability reassignment above)

Same user report as the dmg/cost fairness pass ("附加效果也不平衡"), extended to cover **abilities** (see pokemon-data skill's "Ability reassignment" section — reassigned pairing, not tuned) and **move-level "extra" fields**. Measured the count of `rider`/`status2`/`bonusVsType`/`ignoreReflect`/`ignoreShield` per Pokémon (the 5 genuinely *optional* fields added across the 07-27/07-31 passes above) — ranged **1 to 10** across the roster (avg 5.35), independent of the already-fixed dmg/cost fairness. **`status`/`selfHeal`/`megaBoost`/`bonusEnergy` were deliberately excluded from this count** — those are tier-*structural* (t1/t2 always get `megaBoost`+`bonusEnergy`, t3 always gets `status` XOR `selfHeal`, per the moveset-generation template), not optional bonuses, and touching them would mean second-guessing the HP-bucket→tier-shape design itself rather than fixing "additional effect" imbalance — out of scope for this request.

**One-shot script, add/remove until every Pokémon hits a fixed target (5, the pre-existing average for this 5-field pool — same "calibrate to current average" principle as the dmg/cost pass's `R=14`)**:
- **Add phase** (for Pokémon under target): repeatedly pick the *least-loaded* move (fewest of the 5 fields already set) and add one tier-appropriate field it's missing — t1 tries `rider`→`status2`→`bonusVsType` in order, t2 tries `rider`→`ignoreReflect`→`status2`, t3 tries `bonusVsType`→`ignoreReflect`→`ignoreShield`. `status2` is only addable if the move already has a base `status` to pair with (skipped otherwise, same "dual status" semantics as the 07-31 rider-pass section above); new `rider`/`bonusVsType` values are derived the same deterministic way as that section (`(id, moveIdx)`-keyed pick from the 9-rider pool / the move's own type's 2x-effective target in `EFF`).
- **Remove phase** (for Pokémon over target): repeatedly pick the *most-loaded* move and strip one field, in priority order `ignoreShield → ignoreReflect → bonusVsType → status2 → rider` (strip the "extra flavor" fields before the more foundational `rider`, which every move template leans on most heavily).
- Applied via the same string-aware move-object text surgery as the other data passes this week — `unset` does a scoped regex removal of `,field:value` (handles both `,field:true` and `,field:'string'` and the nested `,status2:{...}` shape), `set` appends new fields before the move object's closing `}`, same as before.

**Result: every one of 152 Pokémon now has exactly 5** (was 1-10) — verified by recomputing the same count from the written files, `min===max===avg===5`. 117/152 Pokémon needed at least one add/remove; 35 were already exactly at 5 and untouched. Verified via `scripts/verify.js` parity (both engines' `attacks` arrays identical post-edit) plus a live single-player battle: confirmed no rendering crash, move-info popup shows the (possibly newly-added) fields correctly, and a reassigned domain ability correctly switched the active stadium on entry (proving ability-id dispatch genuinely doesn't care which species holds it — the reassignment pass and this pass are independent but were verified together in the same browser session).

## Damage range compressed to 50-150 (2026-08-01, one day after the fairness pass above)

The fixed-ratio-14 fairness fix above successfully removed the 5-7x ratio inconsistency, but user reported the *net effect* pushed overall damage too high (max hit 214) — a fair ratio isn't the same complaint as "numbers feel too big." Asked for an explicit bound: **base move `dmg` now derived purely from its (unchanged) `cost` via linear interpolation into [50,150]**, not a fixed ratio: `dmg = clamp(round(50 + (cost/15)*100 + jitter), 50, 150)`, jitter ±5 (id/moveIdx-derived). **`cost` itself was left untouched this time** — the user specifically said "依照能量消耗調整" (adjust *according to* the existing cost), not "redistribute cost again." This supersedes the `R=14` ratio formula from the section above; `dmg/cost` is no longer roughly constant (a cost-1 move and a cost-15 move now sit at opposite ends of a *fixed 50-150 range* rather than both scaling proportionally with cost) — that's an intentional reversion of part of the prior fairness principle, prioritizing "the number stays in a sane range" over "the ratio is perfectly constant." If a future request reopens the ratio-fairness complaint, these two goals are in tension and need to be explicitly reconciled with the user, not silently re-derived from one or the other.

**Mega postform got the same bounded-range treatment, confirmed in-scope via a clarifying question** (the request only mentioned "base" moves; Mega's `applyMegaMoveset` uses a completely separate generation path — see the section above — so it wasn't obviously included). `MEGA_MOVESET_RATIO` (flat ratio per species tier) was replaced with `MEGA_MOVESET_RANGE = {1:[60,160], 2:[70,175], 3:[80,190]}` — same linear-interpolation-into-a-bounded-range shape as the base fix, using `MEGA_MOVESET_COSTS`'s own `[4,12]` span as the interpolation input (`frac = (cost-4)/8`), with the range itself set modestly above base's 50-150 and scaled by species tier (as Mega's reward, same precedent as the retired ratio version). `MEGA_MOVESET_COSTS=[4,6,9,12]` itself is unchanged.

**⚠️ Superseded again, same day: 50-150 still felt too high.** User came back after seeing it in practice and asked for a further compression to **[40,110]** — same formula shape, new bounds: `dmg = clamp(round(40 + (cost/15)*70 + jitter), 40, 110)`, `cost` still untouched. **Mega's range was proportionally pulled down too, without being asked again this time** — the clarifying-question precedent from the first compression pass already established that Mega tracks base, so leaving it at the old [60-190]/[70-175]/[80-190] while base dropped to 40-110 would have been an obvious, glaring mismatch (Mega hitting 70%+ harder than base's new ceiling). New `MEGA_MOVESET_RANGE = {1:[50,120], 2:[55,130], 3:[60,140]}` — kept the same "modest premium over base, scaled by species tier" shape, not a literal copy of base's new numbers. **If base's range changes a third time, apply the same proportional-premium adjustment to Mega automatically rather than waiting to be asked** — this is now an established pattern for this pair of formulas, not a one-off.

Verified: recomputed all 608 base moves' `dmg` post-write — exactly `min=50, max=150`. Mega postform verified live via `applyMegaMoveset()` called directly against one Mega-capable species per tier in both `pokemon_battle.html` (browser) and `server.js` (Node, same extraction technique as prior passes) — dmg values identical between engines, all within `[tier's rangeLo, rangeHi]`. One real single-player battle screen also checked visually to confirm every displayed move's power number sits in the new expected band.

## Status roll decoupled from damage + a real server.js structural bug found (2026-08-01)

User reported a labeled "燒傷 40%" move never seemed to proc burn no matter how many retries. Investigation found the core RNG/roll logic itself was statistically correct in both engines (Monte Carlo: 1000-2000 trials per status type, all landing within normal variance of 40%) — but the user's own hypothesis (surfaced when they were offered "the move might not have dealt damage in your matchup" as one of several possible explanations) turned out right, and asking about it surfaced a **real, previously-undetected bug specific to `server.js`**, not just a design nitpick.

**The fix requested**: status infliction should roll regardless of whether the move actually dealt damage — previously gated on `dmg > 0`/`damage > 0` (originally added so a fully-blocked/resisted hit couldn't also inflict a status, but the user judged this not worth the confusion it caused). `pokemon_battle.html`: removed the `dmg > 0 &&` prefix from the 4 call sites (`tryInflictStatus(atk,...)`, the `wasDoubleStrike` reroll, the `atk.status2` dual-status reroll, and the `iceHowlFreezeThisHit` extra freeze roll) — `tryInflictStatus()` itself still has its own `target.cur <= 0` guard, so a genuinely-dead target is still safe.

**The `server.js` fix required an actual structural refactor, not just deleting conditions** — this is the part worth remembering. `doAttack`'s damage computation is an `if (mult === 0) { damage = 0; ... } else if (frostArmorProc) { damage = 0; ... } else { /* full damage formula + mega-gain + ability-trigger logs + selfHeal + megaDraw/megaSeal + rider switch + the ENTIRE rollStatus closure and its 4 call sites */ }`. **The whole status-roll block was nested inside that `else` branch** — meaning on full type immunity (`mult===0`) or a Frost Armor block, execution never reached `rollStatus` *at all*, regardless of any `damage > 0` check inside it (removing that check alone did nothing, since the code was unreachable, not merely gated). `pokemon_battle.html`'s equivalent uses `let dmg = (mult === 0 || frostArmorProc) ? 0 : Math.max(...)` — a **ternary**, not an if/else block — so the rest of its function body was never inside a conditional scope to begin with, which is why the client-side fix (just deleting `dmg > 0 &&`) was sufficient there but silently insufficient for the server. **Two engines implementing "the same" formula with a ternary vs. an if/else-with-a-large-body are not actually structurally equivalent** — a condition that's cosmetic in one (just picks the value) can gate reachability entirely in the other.

**Fix**: cut the entire status-roll block (the `rollStatus` closure definition + its 4 call sites, ~78 lines) out of the `else` branch and moved it to right after the whole `if/else if/else` closes (immediately before `// Consume buffs`), so it now runs unconditionally regardless of which damage branch was taken. Mega-gain, ability-trigger logs, `selfHeal`, `megaDraw`/`megaSeal`, and the `rider` switch were all **deliberately left inside the `else` block, still gated on `damage > 0`** — the user's request was specifically about the status *roll* ("命中且造成傷害才會骰" refers to the dice-roll language used for status specifically), not a request to make every on-hit side effect damage-independent; scope discipline matters here since e.g. granting Mega energy or self-healing off a fully-resisted hit would be a real balance change nobody asked for. The "Fire thaws freeze" block (fire-type move melting an existing freeze) was also left untouched/still damage-gated for the same reason — thematically that's "you dealt fire damage," unrelated to the status-*infliction* roll being fixed.

**How this was found**: not by code review — by literally testing the exact scenario one of the diagnostic-message's candidate explanations described (electric-type move vs. ground-type defender, `mult=0` immunity) via a live `doAttack()` call in the browser (single-player: worked immediately after the ternary fix) and the same scenario run through an extracted `server.js` `doAttack()` copy in Node (server: still failed after the naive `damage > 0` removal, prompting the deeper investigation that found the if/else nesting). **When a user's own "have you checked X" guess turns out to explain a bug, verify it explicitly with a targeted repro rather than assuming the general Monte Carlo pass already covered it** — a large-sample statistical test of the *isolated* roll function proved the dice was fair, but said nothing about whether the dice was even being rolled in every code path that should reach it.

## PvP-only comeback rule + 24-stadium full redesign (2026-08-13)

Two unrelated requests handled in one session: (1) a brand-new PvP-only round-start rule, (2) a from-scratch rewrite of every one of the 24 existing `cat:'stadium'` cards' effects (not new cards — same 24 ids, entirely new mechanics per card). This is the largest single-session change to the stadium system to date; several established patterns from earlier stadium passes (documented above) were deliberately superseded, not just extended.

**PvP comeback rule**: in `drawForRole(G, role)` (server.js only — confirmed explicitly out of scope for single-player, since the user's request said "雙人對戰額外規則" and single-player already has no symmetric-opponent concept this would apply to), if `role` has exactly 1 Pokémon alive and the opponent has 2+, fully heal `role`'s active and push a bonus supporter card via the existing `pickSupporterAvoidingDupes()`. No "once per game" cap — fires every qualifying turn-start, confirmed intentional (recurring comeback mechanic, not one-shot). Full heal bypasses `isHealSealedSrv` deliberately — treated as a structural/comeback rule, not a "heal effect" going through the normal heal-blocking pipeline, same carve-out precedent as Mega Evolution's full-heal-on-transform.

**Full-replacement semantics, confirmed via AskUserQuestion before implementing**: when a user hands you a complete new description for something that already has an implementation (not a delta like "add X" or "remove Y"), the new text is the card's entire new effect — anything from the old version not mentioned in the new text is gone. This was confirmed explicitly for this pass (several old flat-bonus/mult terms like 龍之谷's +35, 反轉世界's +25-if-still-effective, 羅馬鬥技場's ×1.2, 岩石地帶's steel coverage were all silently dropped per the new spec, not preserved) — don't assume this generalizes to every future "here's the new text" request without checking, but it's the right default reading for "here is a complete redesign" framing specifically (as opposed to "移除+額外" wording, which has its own different default — see that entry below).

**Turn-transition "op/role" anchor, confirmed via AskUserQuestion**: several new effects have an unstated directional subject ("該回合玩家的寶可夢回復...", "回合結束時回復150HP" with no subject, "若對手寶可夢..."). Established anchor: at the point a turn-transition resolves, `op` = the side whose turn just ended, `role`/`role`'s occupant = op's opponent = "對手" in these effects' text. This required restructuring the ambient-effect blocks in both engines:
- **server.js**: `op` had to be hoisted from where it was previously declared (partway through `drawForRole`, after the old symmetric-both-sides ambient block) to the very top of the function, since the new op-anchored effects need it immediately. Removed the old duplicate declaration further down rather than leaving two `const op = ...` statements in the same function scope (would be a redeclaration error).
- **pokemon_battle.html / public/single.html**: no single shared "turn transition" function exists — mirrors the established dual-block pattern (one block in `drawAfterTurn()`, a second, textually-duplicated block at the top of `cpuAttack()`), each computing its own local `stadiumOp`/`stadiumRole` pair (`drawAfterTurn` fires when player's turn starts → op=cpu, role=player; `cpuAttack` mirror fires when CPU's turn starts → op=player, role=cpu). Any future op/role-anchored stadium effect needs adding to **both** of these blocks, not just one — same class of omission risk as every other single-player dual-block pattern in this file.
- **Type-conditioned symmetric effects** (heal/discard triggered by "a Pokémon of type X is on the field", not directional) stay on the older `for (const r of ['p1','p2'])` / `[stadiumOp, stadiumRole].forEach(...)` shape — don't force these into the op/role anchor just because the new pass introduced it; the two shapes coexist and serve different phrasings (evil-forest/toxic-field/shrine's "我方[type]寶可夢回復" is symmetric-both, dark-curse's "若場上為[type]...對手棄牌" is symmetric-check-but-directional-target, hot-springs/training/reversal/ocean's "該回合玩家" is purely op-anchored).

**Two new stadium-scoped lookup tables, replacing hardcoded single-card checks**:
- `STADIUM_DODGE = { stadiumId: { types: [...], chance: N } }` — dodge chances now differ per card (疾風之翼 50%, 劇毒領域 20%, 沙塵暴 70%, 蟲群巢穴 50%), so the old single hardcoded `Math.random() < 0.5` boolean became a table lookup. Both engines.
- `STADIUM_DOUBLE_ATTACK = { stadiumId: matchingAtkType }` — generalizes the double-attack-second-hit-×0.4 mechanic (previously colosseum-only, `atk._halfDamage`/fixed ×0.5) to 3 stadiums (colosseum/fighting, ghost-curse/ghost, mystic-space/psychic) sharing one wrapper function (`attackWithStadiumDouble()` client-side; server.js keeps the mechanic inline in the `attack` handler but now checks the table instead of a hardcoded id, and the pending-second-hit-after-KO-replacement field kept its old name `PendingColosseumHit` for minimal diff even though it's no longer colosseum-specific — cosmetic, didn't rename). The flag/field renamed from `_halfDamage`(fixed 0.5) to `_secondHitMult`(read via `?? 1`, set to `0.4`) in the final damage formula's multiplier chain, both engines.

**New `isStatusCureBlocked(effectType)` / `isStatusCureBlockedSrv(G, effectType)`** generalizes what was previously a single hardcoded "亡靈墓園 blocks everything" check at 3 call sites (`nurse`/`antidote`/discard-trade's cure branch) into a per-status-type helper: ghost-curse still blocks all, but toxic-field now blocks poison-only and lava blocks burn-only — same shape as `isHealSealed`. Each of those 3 call sites now checks `.status`/`.status2` **independently** (a Pokémon poisoned+asleep under toxic-field can still have the sleep cured, only the poison sticks) rather than the old single all-or-nothing branch.

**Damage-formula terms removed/added, not just retuned** — a reminder that a "redesign" pass (unlike a "buff/nerf the numbers" pass) can delete formula terms entirely, which the domain-ability double-counting guards (`abilityDomainBonusApplies`/`!rAbilityDomainBonusApplies`) need re-auditing against: removed `colosseumMult`, `mysticSpaceMult`(old ×0.75 form), `oceanMult`(electric), `oceanPokeBonus`(species-based water), `dragonValleyBonus`, `invertBonus`, `ghostCurseMult`, and the old universal `steelFortressReduction`(-20 both sides regardless of type); added `mysticSpaceReduction`(flat -50, psychic defender), `oceanMoveBonus`(+20, water **move** type not species — see next paragraph), `rockFieldBonus`(+50, rock/ground move type), `iceTundraReduction`(flat -50, ice defender), and a type-scoped `steelFortressReduction`(-50, steel defender only, replacing the universal one). Both engines' main formula AND their separate reflect-mirror formula (`r`-prefixed locals) needed every one of these changes independently — the reflect path is a hand-duplicated copy of the main formula by long-standing deliberate design (see this skill's Reflect section above), not something a shared-formula refactor would have caught automatically.

**Move-type vs species-type convention conflict, caught and self-corrected during this pass**: initially implemented 岩石地帶's new "岩石／地面寶可夢，攻擊造成的傷害+50" using species-type (`attacker.type==='rock'`), following the shape of the old defense-side reductions in the same card. Re-reading the established precedent (this skill's 熔岩火山 entry: "the user's Chinese phrasing for different cards inconsistently says '系寶可夢' vs '屬性的技能' — read literally those would imply different bases, but this game already established the move-type reading for this class of effect") caught the inconsistency before shipping — 岩石地帶's new attack-side bonus uses the exact same grammatical shape as 海洋世界's "水屬性寶可夢招式造成的傷害+20" (also implemented move-type-based, `atkType==='water'`), so it was corrected to move-type too. **The generalizable rule, now confirmed across two separate cards in the same pass**: for this class of ambiguous phrasing, **attack-dealt bonuses read as move-type-based; defense-taken reductions read as species-type-based** (matching how thick-fat/solid-rock-style abilities work) — reach for that split before assuming a single card's wording settles it.

**Tool-independent bug found and fixed while porting, unrelated to the stadium/comeback work**: 熔岩火山's new fire-move-always-inflicts-burn effect needed inserting right next to the pre-existing "fire thaws freeze" hook (`if (dmg > 0 && atkType === 'fire' && ...)`) in `doAttack` — a reminder that a stadium's on-hit side effect (as opposed to a formula term) has its own separate insertion point late in the function, after damage is finalized and applied, not up in the formula-building section with the flat bonuses/mults.

Verified: `pokemon_battle.html` and `server.js` both now apply status (paralysis, `chance:1`) on a `mult=0` full-immunity hit with `damage:0` confirmed unchanged — tested live in-browser for the client and via the same extracted-source Node technique for the server. Re-ran the full 6-status Monte Carlo (2000 trials each) on the *restructured* server.js to confirm the normal (non-immune) path still procs at ~40% and wasn't broken by the code move. `scripts/verify.js` passed throughout.

## Ability-id splitting for per-Pokémon rebalance requests (2026-08-14)

User sent ~22 lines of `特性名稱,新效果` pairs (e.g. "毒療, 擁有中毒狀態，並且中毒時每回合回復 70HP，而不扣血"). Critical constraint discovered before implementing: **this codebase deliberately shares one `ability.id` across many Pokémon with different flavor `name`/`desc`** (e.g. `guts` id is held by 崩潰/毅力/大力士/驚人怪力/硬爪/惡夢/堅韌 etc. — a per-name rebalance request targeting only "堅韌" must NOT touch the other ~6 holders of the same id). Applying the new effect straight to the existing shared id would have silently rebalanced every other Pokémon sharing it too.

**Discovery pitfall, caught before it caused a wrong implementation**: a first pass built a summary table by deduplicating `POKEMON` entries by `ability.id` (keeping only the first-seen name) — this silently mis-attributed which Pokémon actually holds which flavor name (e.g. showed 鐵螯龍蝦 as holding "妖精氣場" when its real ability name is "適應力"). Fixed by dumping the **full** `(id, name, form, flavor-name, desc)` tuple list for every Pokémon (via `new Function('return '+arrSrc)()` on the extracted array, sorted by id) instead of a deduplicated one — always do the full dump when the request is per-flavor-name, not per-id.

**Fix pattern**: for each targeted flavor name, create a brand-new `ability.id` (e.g. `guts` → `guts-cure-burst` for just 堅韌's 4 holders; `pressure` → `pressure-drain` for 壓迫感's 6 holders; `poison-heal`'s single holder just got its existing id's effect changed in place since it wasn't shared) and re-point only that flavor's `POKEMON` entries at the new id via targeted `Edit` calls. Every `doAttack`/`triggerOnEnter`/status-hook branch that used to check the old shared id needs a parallel branch for the new id (not a modification of the old branch, unless the old id's *remaining* holders should also change — check case by case).

**Stale hardcoded ability-name log text, a bug class this split reliably produces**: some log lines hardcode a specific flavor name for a shared id (e.g. `"...的堅韌發動..."` for what was then the `guts` id). After splitting the id that flavor name away, the *remaining* holders of the old id get a wrong hardcoded log line. Grep every hardcoded flavor name in log strings against the id you just split before considering the split done; fix by switching to `${attackerAbility.name}` (already the established pattern for `desperate-blade`'s log line) — but only fix the lines your split actually broke, don't scope-creep into fixing pre-existing unrelated drift (e.g. huge-power/大力士 and tough-claws/硬爪's log lines had the same latent staleness from a much older change, left alone as out-of-scope).

**Syncing pure ability-data changes across 3 files fast**: when a change is confined to the `POKEMON` array's `ability`/`mega.ability` fields (no `attacks`/`type`/`hp` changes), don't hand-edit `server.js` and `public/pvp.html`'s separate `POKEMON` copies to match — first confirm (via a quick Node diff script comparing every non-ability field) that the arrays are otherwise byte-identical across files, then do a **wholesale regex-replace of the entire `const POKEMON = [...]` block**: extract the array literal text from `pokemon_battle.html` and splice it into `server.js`/`public/pvp.html` in place of their own array text. Far more reliable than re-typing 150+ entries by hand or writing a field-by-field merge script — this only works when you've verified there's nothing engine-specific inside that array (there wasn't; sprite paths, stats, and movesets are identical across all three files by existing convention). `scripts/verify.js`'s POKEMON-parity check (JSON.stringify comparison of `attacks`/`ability`/`mega` per index) confirms the result.

**Testing `server.js`'s real functions without starting the WebSocket server**: copy `server.js` to a temp file *inside the repo* (not `/tmp` — Node's `require` resolution for `node_modules` walks up from the file's own directory, so a copy outside the project tree fails with `MODULE_NOT_FOUND` for `express`/`ws`), regex-strip the `server.listen(PORT, ...)` block, and append `module.exports = { doAttack, drawForRole, triggerOnEnterSrv, ... }` before `require()`-ing it. This runs the actual production functions (not a hand-reimplemented mock) against synthetic `G`/Pokémon/buff objects built with the real `freshBuff()` — caught a real bug this way (see next paragraph) that grep-based consistency checks between the two files could not have caught, and reproduced every single-player-verified damage number (154/154/184/20/0/170) exactly. Delete the temp copy from the repo root when done.

**Real bug this technique caught**: a first attempt at porting the new `guts-cure-burst` turn-start hook into `drawForRole(G, role)` called `log.push({...})` — but `drawForRole` takes **no `log` parameter** (confirmed by grepping the whole function body: every other turn-transition stadium effect in it — 訓練場/地熱溫泉/逆轉鬥技場/永凍冰原/邪惡森林/莊嚴神社/沙塵暴/海洋世界/妖精結界原野 — silently mutates state with **no log message at all**, an established convention for this function, not an oversight). The added line would have thrown `ReferenceError: log is not defined` on the very first qualifying turn-start in any live PvP match, invisible to `node -c` syntax checking and to `scripts/verify.js`. Caught only by actually invoking `drawForRole()` in the Node harness above. **Lesson: any new addition to `drawForRole` (or any function without a `log` param) must stay silent like its siblings, not assume `log` is in scope just because sibling functions elsewhere in the file have one.**

## First "pause mid-attack for a real player choice" mechanic — 帕路奇亞「空間切割」(2026-08-14)

Same-day follow-up batch to the ability-id-splitting pass above (揚沙/帝牙盧卡/帕路奇亞/新增騎拉帝納). The first three reused already-established patterns directly:
- **揚沙**(班基拉斯, split off `solid-rock`) and **反轉世界**(騎拉帝納 id 487, new roster entry) are both a plain onEnter-stadium-switch + passive %-dodge id — copy-paste of the same-batch `shock-stadium-dodge` shape (just different stadium target + dodge %). Zero new mechanics.
- **帝牙盧卡「時間咆哮」** needed two genuinely new pieces: (1) "steal **all** items opponent played last turn" — the existing single-object `G[side+'LastItemPlayed']` (used by 複製/trace) only keeps the most recent one, so a **new parallel array field** `G[side+'ItemsPlayedThisTurn']` was added alongside it (pushed to on every item play, reset to `[]` at the start of *that side's own* turn, read-and-cleared by the opponent's `time-roar` holder at the start of *their* turn — the read always happens on a not-yet-reset array since only the array's own owner's turn-start clears it). (2) "100% dodge a would-be-lethal hit, once per game" — a `poke._timeRoarDodgeUsed` one-shot flag, exact same shape as `_healingRainbowUsed`, checked via `dmg >= defender.cur` (not `>`) among the other passive-dodge checks (coinShield/abyssDodge/stadiumDodge) right before `defender.cur -= dmg`.

**帕路奇亞「空間切割」** is the interesting one — the first ability in this game's main battle mode (not Pocket TCG, which already has a `needsChoice`/`pendingChoice` system) that needs a genuine **pause-and-wait-for-player-input in the middle of an attack**, for its "受到攻擊時，可以棄掉當前的競技場卡，傷害-50" clause. Confirmed via AskUserQuestion that the user wanted a *real* confirm/decline prompt, not an auto-decided simplification — this was explicitly the more expensive of the two options offered, chosen anyway.

**Split into two independent halves, only one of which needed the new interrupt machinery**:
- **"Opponent can't activate any stadium while I'm active"** — no new pattern needed. One helper (`spaceCutBlocks(actingSide)` / `spaceCutBlocksSrv(G, actingRole)`, mirrors the existing `isHealSealed`/`dark-abyss-lockdown` "check the opponent's active ability" shape) gates every place a stadium can change: the player-manual `useTrainer`/`use_trainer` stadium-card-play check (before consuming the card, same spot as the existing `SupporterLockedThisTurn` check), and **all 6** `triggerOnEnter`/`triggerOnEnterSrv` auto-switch blocks (`shock-stadium-dodge`, `sandstorm-stadium-dodge`, `reverse-world-dodge`, `drizzle-ocean`, `drought-lava`, the generic `DOMAIN_ABILITY_STADIUM` lookup) — CPU never plays stadium cards via its own AI (`cpuUseTrainers()`'s `useCard()` closure has zero stadium-id call sites, confirmed by grep), so no CPU-side manual-play guard was needed, only the onEnter auto-switch guards.
- **"May discard the active stadium for -50 on the incoming hit"** — this is the real new piece. Design: **keep `doAttack` itself untouched except for one read-and-clear flag** (`spaceCutReduction = G[dSide+'SpaceCutBonus'] || 0; G[dSide+'SpaceCutBonus'] = 0;` at the very top, subtracted in the final flat-reduction chain alongside `rockFieldReduction`/`thickFatPureReduction`/etc.) — the entire "ask, wait, decide" flow happens **before** `doAttack` is ever called, in the orchestration layer, so `doAttack`'s single-pass synchronous formula never needs to be paused mid-calculation.
  - **Single-player**: new `resolveSpaceCutChoice(attacker, defender, aSide, dSide, onResolved)` gate function, called right before every `attackWithStadiumDouble(...)` call site (both `playerAttack`'s and `cpuAttack`'s). If defender isn't a space-cut holder or no stadium is active, it's a same-tick passthrough (`onResolved()` called immediately, zero behavior change for every other Pokémon in the game). If CPU is defending: **auto-decide, always discard** — deliberately the simplest possible rule (matching this game's established "CPU AI stays simple" precedent throughout — heal-below-40%, cure-first, etc. — no lookahead about whether CPU might rather keep its own beneficial stadium). If the player is defending: opens a new `#space-cut-modal` (built from the same generic `.discard-inner`/`.discard-title`/`.discard-sub`/`.btn-modal-ok`/`.btn-modal-cancel` classes every other confirm modal in this file already uses — see the modal-CSS-pair note in `adding-cards` skill, both the base `display:none` and `.open{display:flex}` rules needed adding to the *shared* selector lists at the top of `<style>`, not a standalone new rule block) and genuinely pauses — the callback is stashed in a module-level `_spaceCutPendingCallback`, invoked from the button's `onclick` handler (`resolveSpaceCutChoiceUI(discard)`), which is the only thing that lets `attackWithStadiumDouble`/`doAttack` actually run.
  - **PvP**: no per-connection "CPU" shortcut exists — both sides of every room are real WS clients, so *every* space-cut-holding defender needs the real pause, no auto-decide branch at all. This required an actual refactor of the `attack` WS handler: the back half of the handler (everything from `const switchGuardMult = ...` through the final `broadcast(...)`/`return` — computing `switchGuardMult`/`standbyGuardMult`, spending energy, calling `doAttack`/`attackWithStadiumDouble`'s double-hit logic, the attacker/defender-died KO branching) was extracted verbatim into a new top-level function `resolveAttackExchangeSrv(room, G, role, op, attacker, defender, atk, atkCost, aBuff, dBuff, log)` — extraction was safe because `broadcast`/`endGame` are already **module-level** functions (not closures captured per-connection), and `room`/`G`/`role`/`op` were just passed through as ordinary parameters. The `attack` handler now does its usual setup (energy check, `sResult.died`/`sResult.skipped` early returns unchanged) then, right before what used to be the `switchGuardMult` line, checks `if (!isAbilitySealedSrv(op, G) && defender.ability?.id === 'space-cut' && G.activeStadium)`: if true, stashes the *minimal* resumable state (`G.pendingSpaceCutChoice = { role, op, atkIdx: msg.idx, atkCost }` — role names and an index, not object references, matching how `pendingKOSwitch`/`pendingKOSwitchQueue` already only ever store role strings) and returns without calling `resolveAttackExchangeSrv` yet; otherwise it calls it immediately (the common, no-space-cut-defender case — zero added latency/behavior for every other matchup in the game). A new `space_cut_choice` handler (validates `role === pending.op` — only the actual defender's own connection can answer) re-derives `attacker`/`defender`/`atk`/`aBuff`/`dBuff` fresh from `G[pending.role/op + 'Deck'][...Idx]`/`attacks[pending.atkIdx]` (never trusts stale references), applies the discard-or-not (clearing `G.activeStadium` + setting `G[pending.op+'SpaceCutBonus']=50` only if `msg.discard`), clears `G.pendingSpaceCutChoice`, then calls the exact same `resolveAttackExchangeSrv(...)` to actually finish the attack. **`G.pendingSpaceCutChoice` needed adding to every existing `G.pendingKOSwitch` gate** (`mega_evolve`/`attack`/`standby`/`switch`/`discard_trade`/`use_trainer` — 6 call sites, all of the shared shape `if (G.turn !== role || G.pendingKOSwitch) return;` → `|| G.pendingSpaceCutChoice`, found via grepping the exact string across the whole battle-message handler section) so neither side can slip in another action while the choice is outstanding — same "gate every turn-action behind the pending sub-decision" precedent `pendingKOSwitch` already established, just a second independent pending-state variable layered on top, not replacing it.
  - **`public/pvp.html`** needed a parallel modal (`#space-cut-modal`, same generic modal classes) plus: (a) in the `case 'update':` WS message handler, check `msg.state.pendingSpaceCutChoice?.op === myRole` (only the actual defender's own client shows the prompt; spectators and the attacker's own client never do) and call `showSpaceCutChoiceModal(stadiumName)`, else `closeSpaceCutChoiceModal()` (handles the state clearing itself once resolved); (b) extended the existing `G.busy` derivation (`const opKO = sG.pendingKOSwitch...`) with `|| !!sG.pendingSpaceCutChoice` so normal battle-action buttons stay disabled for both clients while the choice is outstanding, mirroring the KO-switch case exactly; (c) a `_spaceCutShown` guard flag prevents the modal from re-triggering/flickering if further `update` broadcasts land while it's already open (same pending state re-broadcast more than once is possible depending on what else changes `G` before the choice resolves).

**Verification**: this was the first change in this session's two-part ability batch where a pure Node-extraction test of `doAttack` alone wasn't enough — also had to call the *real* `resolveAttackExchangeSrv` (exported from the same server.js-copy-with-listen-stripped harness used throughout this whole session) end-to-end with a pre-set `G.p2SpaceCutBonus` to confirm the extraction didn't drop or mis-scope a variable when the inline code became a standalone function (it hadn't — ran clean, `G.turn` flipped correctly, damage matched the hand-derived expected value exactly). Single-player's full click-through was verified in a live browser tab: injected a synthetic space-cut-holding player Pokémon, called the real `resolveSpaceCutChoice` the same way `cpuAttack` would, confirmed the actual DOM modal opened with the correct dynamic stadium-name text, then dispatched a real `.click()` on the "discard" button and confirmed `G.activeStadium`/`G.playerSpaceCutBonus`/the modal's `.open` class all updated correctly — not just the underlying JS state, the actual UI wiring.

## 帕路奇亞「空間切割」reactive half reverted same day; 帝牙盧卡 tuned to random-half; PvP first-onEnter-order bug fixed (2026-08-14, later same day)

Immediately after shipping, the user asked for two of the four abilities above to change again, plus reported two separate issues:

**帝牙盧卡「時間咆哮」: "獲得對手上回合用過的所有道具卡" → "…隨機一半"** — the item-steal array (`G[side+'ItemsPlayedThisTurn']`) is now shuffled (`.sort(() => Math.random()-0.5)`, this codebase's established ad-hoc shuffle idiom — no dedicated Fisher-Yates helper exists here) and sliced to `Math.ceil(length/2)` before being pushed into the thief's hand, in all 3 places that read it (`cpuAttack`, `drawAfterTurn`, `drawForRole`). Odd counts round up (3 items → steals 2) — an arbitrary but reasonable default, not confirmed with the user; if this specific rounding direction ever matters to them, ask. The *other* (non-stolen) half is simply never duplicated anywhere — these are copies of already-spent cards, not cards taken away from anyone, so there's no "return the rest" step needed.

**帕路奇亞「空間切割」: the reactive "受到攻擊時可棄卡換-50" clause fully reverted, replaced with "上場時，清除當前的競技場效果"** — a plain, unconditional onEnter `G.activeStadium = null` (no `spaceCutBlocks` guard needed, since *clearing* isn't "activating" a stadium). This **completely removed the interrupt-mid-attack machinery** documented in the section immediately above, same day it shipped: `resolveSpaceCutChoice`/`resolveSpaceCutChoiceUI`/`_spaceCutPendingCallback` and `#space-cut-modal` (single-player), the `spaceCutReduction` read-and-clear + formula term in `doAttack` (both engines), the `pendingSpaceCutChoice` pause-check + `space_cut_choice` handler + the 6 `|| G.pendingSpaceCutChoice` turn-action gates + `public/pvp.html`'s modal/`G.busy` wiring (PvP). **`resolveAttackExchangeSrv` (the extracted back-half of the `attack` WS handler) was kept** even though its original motivating reason is gone — it's a reasonable decomposition on its own merits (the `attack` handler is still very long), and re-inlining it would be pure churn with no behavior change, just risk. The "opponent can't activate any stadium while I'm active" half of the ability (`spaceCutBlocks`/`spaceCutBlocksSrv`, the 6 `triggerOnEnter`/`triggerOnEnterSrv` stadium-switch guards, the `useTrainer`/`use_trainer` stadium-card-play block) was **not** touched — only asked to change the reactive-discard clause, this half stays. **Lesson reinforced**: when a user walks back a feature the same day it ships, check whether the request is a full revert of one clause or the whole ability — here it was scoped to exactly one of the two independent clauses (confirmed by the request's wording naming the specific old clause being replaced), so the other clause and its supporting code stayed completely untouched.

**Real bug found while investigating a *fourth*, seemingly-unrelated user report** ("mega班基拉斯在場時，暗夜詛咒領域的回合結束棄牌效果沒發動") — turned out to be a *consequence*, not a code bug in the discard mechanism itself. Isolated test confirmed 暗夜詛咒領域's turn-end discard fires correctly whenever it's genuinely the active stadium and a dark-type is present, mega-evolved or not (`applyMegaEvolution`/`megaEvolve` correctly mutates `poke.type`/`type2` to the Mega form's — confirmed by reading the function, Mega Tyranitar is rock/dark same as base, so type-detection was never the issue). The actual mechanism: **班基拉斯's own ability (揚沙, this same day's earlier batch) unconditionally overwrites whatever stadium is active with 沙塵暴 every time it *enters* the field** — switch-in, initial deploy, KO-replacement, or Mega-evolving-in-place (`applyMegaEvolution`'s `triggerOnEnter(poke, side, false)` call fires ability effects even though `isFieldEntry=false` only suppresses *trap*-stadium damage, not the ability's own onEnter logic) — so if 暗夜詛咒領域 was active and 班基拉斯 then entered/evolved, it gets silently replaced, and the discard effect appears to stop firing because it's no longer the active stadium, not because the effect is broken. This is **揚沙 working exactly as the user specified it earlier that same session** ("上場時場地切換為沙塵暴") — flagged to the user as a finding to confirm/resolve rather than silently "fixed," since silently changing one of two conflicting same-day requests without asking which one should yield is exactly the kind of substitution this project's `feedback_dont_silently_substitute_requests` memory warns against.

**Second, genuinely independent bug found and fixed while investigating the user's onEnter-order question**: `buildG()` (server.js, PvP battle setup) rolls a coin flip for who acts first (`firstTurn = Math.random()<0.5 ? 'p1':'p2'; G.turn = firstTurn`) but the two starting Pokémon's onEnter triggers were called in a **hardcoded `p1`-then-`p2` order regardless of `firstTurn`** — so the second-turn side's onEnter abilities (including any stadium-switcher) would sometimes fire *before* the first-turn side's, even though the user's expectation ("先手的玩家先發動") is that whoever wins the coin flip triggers first. Fixed by deriving both the log-order and the actual call order from `firstTurn`/its complement instead of the literal role strings. **Single-player has no equivalent bug** — no coin flip exists there at all (player always acts first, confirmed by grep finding zero 先攻/後攻/coinFlip references in `pokemon_battle.html` outside of unrelated ability flavor text), so `startBattle()`'s hardcoded `triggerOnEnter(P())` then `triggerOnEnter(CP())` order was already correct as-is and didn't need touching. **Verified via the same live-instrumentation trick used throughout this session**: gave both sides a `pressure`-clone ability with distinct Pokémon names, ran `buildG()` 40 times, and asserted `startLog[0]`'s text always names whichever side `G.turn` ended up being — 40/40 correct after the fix (first attempt at verifying this by checking `G.activeStadium`'s final value instead of the actual firing order was a **false-positive-prone test design** — both sides' onEnter always fires regardless of turn order, so checking "did an effect fire" proves nothing about *order*; only checking the relative position of each side's own log message inside the shared `log`/`startLog` array actually tests ordering).
